# Phase 5: Frontend And Bytecode Emitter

Status: completed

## Goal

Port QuickJS frontend behavior and emit bytecode directly. This phase must not
recreate the old AST interpreter or any standalone execution path.

## QuickJS References

- `quickjs/quickjs.c` lexer, parser, scope, function parsing, class parsing, destructuring, eval, module, and bytecode emission sections.
- `quickjs/quickjs-opcode.h` for emitted opcode validation.

## Target Files

- `src/engine/frontend/token.zig`
- `src/engine/frontend/lexer.zig`
- `src/engine/frontend/parser.zig`
- `src/engine/frontend/regexp_literal.zig`
- `src/engine/frontend/source_pos.zig`
- `src/engine/bytecode/emitter.zig`
- `src/engine/bytecode/scope.zig`
- `src/engine/bytecode/function.zig`
- `src/engine/bytecode/module.zig`
- `src/tests/frontend/all.zig`
- `src/tests/bytecode/all.zig`

## Coverage Matrix

Detailed syntax-domain tracking lives in
`../../matrices/frontend-coverage-matrix.md`. Update matrix rows with fixture paths
and emitted metadata/opcode evidence as features land.

## Work Breakdown

- [x] Port token definitions, keyword handling, private names, and strict/module context flags.
- [x] Port numeric, string, template, and regexp literal lexing.
- [x] Port script, module, function, eval, async, and generator parse entrypoints.
- [x] Port lexical scope and variable binding rules.
- [x] Port closure capture metadata and eval-specific binding metadata.
- [x] Port class parsing, method/accessor parsing, private fields, `super`, and home-object metadata.
- [x] Port destructuring, default/rest/spread, and short-circuit expression emission.
- [x] Port import/export parsing and compiled module metadata emission.
- [x] Emit QuickJS-aligned bytecode through `bytecode/emitter.zig`.
- [x] Add parser/emitter fixtures that inspect bytecode and metadata without running the VM.

## Validation

```bash
zig fmt .
zig build test --summary all
```

Focused fixtures should cover:

- Script vs module parse mode.
- Direct vs indirect eval metadata.
- Function and closure capture metadata.
- Class methods, accessors, private names, static elements, and computed names.
- Destructuring and spread/rest/default emission.
- Import/export records.
- Syntax error reporting with source positions.

## Exit Checklist

- [x] Parser/emitter tests pass without AST execution.
- [x] All non-deferred rows in `../../matrices/frontend-coverage-matrix.md` are `validated`.
- [x] Bytecode output uses opcode metadata from Phase 4.
- [x] Source position and syntax error metadata are tested.
- [x] `status.zig` marks frontend and emitter subsystems as `validated`.
- [x] `TRACKING.md` records validation evidence and known unexecuted bytecode coverage gaps.

## Handoff Notes

Phase 5 validates parser/emitter metadata only. The generated bytecode is not
executed in this phase; semantic execution for all opcode groups remains Phase 6
work and is tracked in `../../matrices/opcode-execution-matrix.md`.
