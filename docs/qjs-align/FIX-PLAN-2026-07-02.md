# zjs → QuickJS 忠实对齐修复方案（2026-07-02）

> 依据：`AUDIT-2026-07-02-full-surface.md`（175 条确认偏离，~110 条已二次人工复核）。
> qjs 参照 = `/home/aneryu/quickjs`（Bellard，`04be246`）；zjs 基线 = `541c30f`。
> 本文件是执行蓝图：切片、顺序、依赖、文件所有权、门禁、工作量、风险、不对齐登记。
> **尚未动工**——按用户指示先出方案。

---

## 0. 原则与门禁（每个切片通用）

0. **主线 = 对齐 qjs 的实现**（用户 2026-07-02/03 两次强调）：目标是代码实现级镜像 quickjs.c，
   行为补丁只是过渡形态。由此：凡以「算法等价 + fuzz 校验」暂代逐行移植之处（B4 radix delta 算法、
   B2 自写 JSON 镜像），其实现级残余（**dtoa.c radix 引擎移植、reviver walk 忠实重写**等）为主线
   backlog 而非可选项；E 系深前沿（异步挂起模型/模块管线/GC 迭代化/私有 brand/生成器完成模型）
   本身就是主线的实现级对齐项。唯一例外见第 2 条红线。
1. **忠实 = 镜像 quickjs.c 的结构与行为**，commit message 注明所镜像的 qjs 函数。
2. **红线**：`test262 0/49775`。当 qjs 行为与 spec/test262 冲突时**不对齐**，登记进
   `DIVERGENCE-CATALOG.md`（§7 no-align 登记）。
3. **门禁阶梯**：
   - 切片内：`zig build zjs` + 该切片全部 repro 探针（zjs 修后输出 == qjs）+ 定向 test262 目录；
   - 集成检查点：`zig build test` + **全量 test262** + force-GC（凡触对象生命周期/迭代器/promise）；
   - 阶段收口：ReleaseSafe + test-altrepr（触 value 表示时）。
   - ⚠️ 记忆教训：worktree 内 agent 跑不了 test262 → **集成后必须在主树重跑全量 union 门禁**；
     gate 构建会覆写 `zig-out/bin/zjs`（force-GC 二进制）→ 任何 perf/行为探针前先 `zig build zjs`。
4. **消息纪律**：所有触碰的 throw 点改用 `throw*Message` helper 并带上 **qjs 的消息原文**
   （对齐 `JS_ThrowTypeError` printf 文本）；不新增裸 error enum。
5. **探针固化（A0，先行交付）**：`/tmp/recheck/*.js`（~110 条）迁入仓库
   `tests/fixtures/qjs_diff/`，作为常驻差分回归 corpus（期望输出以 qjs 实测为准、
   no-align 条目以 zjs 期望为准）。/tmp 易失，这是本审计最有复用价值的资产。

**并行纪律**（沿用 round-8~11 模式）：改文件的 agent 一律 worktree 隔离；**切片按文件所有权划界，
互不触碰**；集成按依赖序 `git apply --check` + cherry-pick；union 全门禁后按切片分 commit。

---

## 1. 阶段总览与依赖

```
Phase A  基础设施（栈守卫 + InternalError）        ← 串行先行，B/D 多数依赖
Phase B  崩溃与数据丢失（红线批）                  ← A 后并行切片
Phase C  解析器批（单一所有权 parser.zig，串行内批）← 与 B 并行
Phase D  行为对齐批（9 个并行切片）                ← B/C 集成后
Phase E  深前沿 keystone（多会话独立立项）         ← D2/D3 之后
Phase P  策略决策点（用户拍板，不阻塞 A–D）
```

建议执行节奏（每步以全量门禁收口）：
- **会话 1**：A0+A + B1 + B3 + B6（最快消红线）；C 启动
- **会话 2**：C 收口 + B2 + B4
- **会话 3**：D1/D5/D6/D7/D8/D9/D10（并行 worktree 批 → union 门禁）
- **会话 4**：D2 + D3 + B7 + D4
- **会话 5+**：E1 蓝图先行 → 实施；E2–E5 依序
- P 组随时一次性问清即可插队执行。

---

## 状态日志

- **known 16→13 续(2026-07-03,recon workflow `wf_d4a4f152-99b` 6 只读 mapper 产出全 16 known 蓝图 → 我串行实现,每条独立门禁 `0/49775`+smoke 3/3)**。蓝图落地 `docs/qjs-align/KNOWN16-RECON-2026-07-03.md`(每簇 root cause+qjs 锚点+exact fix+tractability,深簇实现指南)。
  - **usage-from-eval ✅ `b40a384`**(修 1):import() 在 eval 内的 referrer 取当前函数 filename=编译期哨兵 "<eval>"→相对 specifier 对 cwd 解析 reject。改=走 frame.evalCallerFrame() 链到外层真 script/module(=GetActiveScriptOrModule/JS_GetScriptOrModuleName quickjs.c:30854)。vm_eval_module.zig。
  - **async-gen Cluster A ✅ `f21e690`**(修 2):async gen return 的 await 原是 driver 侧按运行时 `!result.isUndefined()` 判(async_generator.zig),qjs 是编译期按语法 hasval 把 OP_await 烤进字节码(emit_return quickjs.c:28401-28404)。两 bug:`return undefined`(hasval 应 await)被跳→少一 tick;yield-star return(字节码已有 OP_await)被 driver 再 await 一次→多一 tick+多一次 thenable .then。修=三 return-emit 点(main/cond-branch/captured-through-finally)对 async **generator** 发 OP_await + 删 driver `.complete_result` await。plain async fn/隐式 return 不受影响(`in_async and in_generator` 门)。
  - **type-text 判为 no-align(保 known)**:zjs static text import 给 string(实现了 CreateTextModule,过 5 静态测试),dynamic 给 number——但 dynamic 路径 `importLoaderTypeFromAttributes`(module_graph.zig:66-84 enum 只 {none,json})**是刻意 qjs-faithful**(注释引 quickjs-libc.c,qjs dynamic 也给 number)。补 dynamic text 会**背离 qjs**→违主线,故保 known。recon 的「config skip」前提错(以为 qjs 全无 text 支持)。
  - **for-await arguments 尝试(workflow `wf_9c371773-6bc` implement-in-worktree+我集成)→ 回退**:实现干净(parser.zig only,`ensureArgumentsVar` 一次性 add_arguments_var@function-scope+删 per-ref special_object+改 typeof/3 解构赋值目标,parse-time 避开 resolve-size 陷阱;2 目标测试过、typeof/mapped/arrow-inherit/explicit-param/undeclared 全 MATCH)**但全量门禁 5 回归**:`arguments-with-arguments-fn.js`×4 + `block-decl-func-skip-arguments.js`——根因=**缺 spec argumentsObjectNeeded 计算**(19-20:无 param 表达式且 arguments∈functionNames/lexicalNames→**不建** arguments 对象、`arguments` 指 `function arguments(){}`;`ensureFunctionScopeVar(arguments)` 与 function-decl arguments 绑定撞槽→param-initializer 读到 function 而非 object)。**修正蓝图:for-await arguments 需先实现 argumentsObjectNeeded(参数/functionNames/lexicalNames 三集)决定是否 materialize,非「一见引用就 materialize」;且 arguments var 须与 function-decl arguments 区分**。净 known 仍 13(2 fixed 抵 5 regress→回退,cluster 保 known)。
  - **module TLA Steps 1-2 尝试(workflow `wf_17047c69-7c9` implement-in-worktree+我集成)→ 回退**:实测证明 **Steps 1-2 不足、必须全 Steps 1-5**。改动=vm_gen_async.zig awaitSuspendMode 模块体 `.settled`→`.raw`(真挂起,1 行)+ module_graph.zig 驱动器 job-driven resume(+286 行)。**tick-ordering 通过**(top-level-ticks/ticks-2 harness AsyncTestComplete、`["t1","a1","t2","a2"]` 正确交错),**但全量 module-code 目录 10 新 error**——真挂起打破了 TLA **rejection/error 传播**(await-expr-reject-throws/dynamic-import-rejection/await-awaits-thenables-that-throw/async-module-does-not-block-sibling-modules 原本过、现挂),因为旧 `.settled` 同步处理 rejection,新 job-driven 路径缺 Steps 3-5 的 fulfilled/rejected + Tarjan DFS 机件。fixed 2 抵不过 10 regress → 干净回退。**结论:module TLA 是 all-or-nothing——必须一次性 Steps 1-5(js_evaluate_module 返回 promise + js_inner_module_evaluation Tarjan DFS + async_module_execution_fulfilled/rejected,~1000 行,多会话 keystone),不能增量落 tick-only**。qjsAsyncFunctionStart 复用可行(Step 2 已证),缺的是 rejection 传播 + DFS 记账。
  - **两次深簇尝试(for-await/module-TLA)实证:剩余 known 是 hard tail,每个需完整实现非增量;workflow implement-in-worktree + 我主树全量门禁 = 正确风控(worktree 隔离保主树干净,门禁抓 worktree dual-engine 探针漏掉的 broad regression:for-await 5、module-TLA 10)**。
  - **剩余 13 known(全有蓝图):module TLA 7(最大 keystone,深:删 vm_gen_async.zig `.settled` 模式+走已证正确的 qjsAsyncFunctionStart+Tarjan async-DFS 记账)/for-await arguments 2(中:一次性 add_arguments_var@function-scope,但需 resolve 期加 var slot 撞 frame-sizing 风险)/await-using 1(中:4 文件+新 special_object subtype,共享 using dispose chokepoint 高风险)/async-gen Cluster B 1(深:.return()-through-finally 用户 throw 被 findGeneratorReturnFinallyTargetFromCatch 的 stop_pc 裁掉,sync+async 共享)/dynamic-import arrow 1(深:await import(内联 fn)+用户 toString 重入→操作数栈损坏)/import-attributes type-text 1(no-align)**。下一步:module TLA(蓝图 Step1 删 .settled 先拿 2-3 tick 测试)或 for-await arguments。

- **known 24→16 批(2026-07-03,我亲自串行,每条独立门禁 `0/49775` + smoke 3/3)**:4 commit,均代码实现级镜像 qjs。
  - **await 优先级 ✅ `2ed405a`**:`await` 操作数是 UnaryExpression(=js_parse_unary TOK_AWAIT),原按 AssignExpr 解析→`await Promise.resolve(2)*x` 错绑 `await(v*x)`;改 parseUnary。修 2(MultiplicativeExpression + ConditionalExpression)。
  - **Promise.prototype.catch 路由 ✅ `383729d`**:catch 恒 `Invoke(this,"then",[undefined,arg])`(=js_promise_catch quickjs.c:54275-54282),原在真 promise 上走 builtin then-capability 快路径→重排 .then 观察序;全走 qjsPromiseCatchGeneric。修 finally/rejected-observable-then-calls 1。
  - **global-fndecl ✅ `b5ce923`**:script 顶层 `function f(){}` 原装成 lexical JS_CLOSURE_MODULE_DECL closure var(引用 get_var_ref 读被捕获的**陈旧 cell**)→`globalThis.f=x` 不可见。qjs 是 global var(is_global_var→add_global_var 36980)、引用编 dynamic OP_get_var(resolve_scope_var 33272)。**修=给 script 引擎:走 global_vars hoist 表安装(child_decl_emit_global_inline,像顶层 var)+**不建 closure var**→引用落 get_var;模块/直接 eval 保留 MODULE_DECL cell 不变;child-init size/emit pass 跳过 global-inline child(hoist 表装、无 local slot)**。加 `ClosureVar.is_global_fn_ref` 标记(仅 script fn-decl 置位),resolution 两 lookup + ensureClosureVar 捕获全跳过它。收编 harness/asyncHelpers-asyncTest-*(3,它们改写 globalThis.$DONE 经 asyncTest 自由 $DONE 引用观察)。**⚠️两条歧路先撞坑**:strategy B(non-lexical global_decl 共享 global cell)+attempt#1(get_var+global VARREF)——本以为引入崩溃反复 revert 数小时,**真相=run-test262 二进制被频繁 rebuild 腐化 zig-cache(交互 bash `-c` 恒崩、`2605/3001 errors`),干净 `zig build run-test262` 后立即 `0/3001`;引擎二进制始终正常**。见 [[zjs-bench-stale-binary-hazard]]。
  - **Atomics.waitAsync 链 ✅ `1d6e794`**:waitAsync 结果 promise 用 lazy 单 reaction-callback(cross-thread notify 路径),`.then` 存 on_fulfilled 却丢弃 chained capability + atomicsSettleAsyncWaiter 预置 result→settlePendingPromiseReaction 跑首 reaction 后 early-return(result!=null)不 fire reaction list→`waitAsync(...).value.then(a).then(b)` 丢 b、测试到不了 $DONE。修=(1) lazy `.then` 追加 pass-through reaction 绑 chained capability;(2) atomicsSettleAsyncWaiter 有 callback 时不预置 result(callback 经 reaction arg 拿值)→drain 会 fire reaction list。收编 waitAsync 2。**残余(预存,非回归,known 外)**:多个 direct reaction(`value.then(r1);value.then(r2)`)单 callback 设计只跑 r2。
  - **剩余 16 known 簇**:async 作业/tick 模型(async-gen tick 序 3 + TLA 7 = 10,E1 §4 async-suspension keystone,深)、dynamic-import 3(runner/host 需调查)、for-await arguments 2(**已诊断**:顶层 `let/var arguments` 与函数隐式 arguments 竞争、按引用惰性 materialize 非 qjs add_arguments_var 一次性、解析器脆弱)、await-using 1。

- **known 38→24 批(2026-07-03,workflow `wf_e13d746e-75a` 4 切片 + promise handoff 我集成)**:联合门禁 `0/49775, known 24` + smoke 3/3,分支领先 main 105。
  - **import-attributes ✅ `69aecab`**(实现 `evaluateImportCall`=js_dynamic_import 31073 忠实镜像:options 非对象/with 非对象/非字符串值/abrupt 全 reject TypeError + type:json 穿透加载器;修 3 known;它是最慢切片=真特性实现+多轮全量门禁)。
  - **async-residuals ✅ `234a2ae`**(AsyncGenerator.prototype.return 不再同步抛+for-await IteratorClose 异常 reject 请求=js_async_generator_resume_next 21600;修 1)。
  - **message-mapping ✅ `9338a68`+我补 value_ops 传播**(bigint div/mod/pow 不再 remap 到裸 RangeError→`1n/0n`="BigInt division by zero"、`2n**-1n`="BigInt negative exponent" 逐字对齐;message-only)。
  - **promise-combinators:切片 0 修但黄金 handoff**(所有权错分=机件已迁 exec/promise_ops+call.zig,非 builtins/core promise)→**我照 handoff 落地 `de...`**:combinator 结果/errors 数组带 %Array.prototype%(instanceof Array,=JS_NewArray 54012)+executor abrupt 走 reject fn 尊重 alreadyResolved(=js_promise_constructor)+resolve/reject 静态 receiver gate 用 isConstructorLike 收子类(=js_promise_resolve this_val 构造器);修 10 known。**残余:Promise.prototype.finally thenable .then 观察差 1 tick(1 known)**。
  - **教训:并行切片的文件所有权要按「机件实际所在」划,不按「概念归属」——promise 机件在 exec/ 不在 builtins/core,错分导致该切片空转只能 handoff**。**剩余 24 known 簇**:TLA 6(E1 余步 keystone)/harness 3(全局 function-decl 绑定 bug,已登记独立切片)/for-await+async-gen+await ~7(E1 余步)/waitAsync 2/import-attributes 1+dynamic-import 2(残余)/finally 1。

- **✅已修 `b5ce923`(见顶部 known 24→16 批)——下方原诊断的「修法」被推翻**:真根因不是 `defineGlobalFunctionBindingValue` 的 cell 写透传(反汇编证写透传本已正确),而是**解析器把 script fn-decl 装成可捕获 closure var→引用 get_var_ref 读陈旧 cell**;正确修法=script fn-decl 走 global_vars hoist 表安装、不建 closure var、引用落 dynamic get_var(`is_global_fn_ref` 标记)。下方旧诊断保留作历史。**🔬(旧,已过时)新发现待修切片:顶层 function 声明的全局绑定与 globalThis 属性双向脱钩**。根因(双引擎+描述符实证):`function f(){}` 在 script 顶层创建 var_ref cell(自由引用 `f` 经 OP_get_var 读它)**外加一个存独立副本的普通 data 属性**,两者不别名→`globalThis.f=v` 只写属性、`f=v` 只写 cell,读侧各读各的旧值(`g=99` 后 gopd(globalThis,"g").value 仍是原函数=铁证)。qjs 全局函数属性是 `JS_PROP_VARREF`(值槽即 cell,单一存储);zjs 的 var/global_decl 走 VARREF 正确,唯 function-decl 装成 plain-data+孤立 cell(见 `defineGlobalFunctionBindingValue` slot_ops.zig:377——存在 cell 时写 cell,否则 defineOwnProperty 普通 data)。**修法**:function-decl 全局绑定应像 var/global_decl 装成 cell-backed VARREF 属性(mirror qjs js_closure_define_global_var + JS_PROP_VARREF 写透传)。**影响**:test262 harness/asyncHelpers-asyncTest-*(3 known,asyncTest 引用被测试重赋的 globalThis.$DONE 读到旧值)+ 概念上「可变 globalThis intrinsic」族。**敏感度高(A4 33-回归史)→ 独立切片,不并行仓促改**。全量 test262 global-var 覆盖作兜底。

- **B1 ✅ 已提交 `59f6535`**（2026-07-02，test262 0/49775）：`core.string.max_length = JS_STRING_LEN_MAX`，卡点 `createUninitialized`（=js_alloc_string 4078）/`createRope`（=4898）/`appendRopeTail`（越限 bail→createRope 抛）；`error.StringTooLong`→InternalError "string too long"；repeat 镜像 `js_string_repeat`（count>2^31-1→"invalid repeat count"、超限→"invalid string length"，`error.InvalidLength` 于 string_ops 包装点配消息）。双引擎逐字对齐（含消息）；perf A/B 噪声内零回退。
- **B4-slice ✅ 已提交 `f2fcf97`**：Math.random 真实现（xorshift64star=quickjs.c:47362 + [1,2) 位打包=47383 + 微秒种子=js_random_init 47373；原是常量 0.5 桩）。test262 0/49775 + smoke 3/3。B4 其余（toString(radix)/round-trip band/parseInt 精度/Number('0x…')溢出/toLocaleString 参数）未动。
- **reviver-walk ✅ + message-sweep(string 部分)✅**（2026-07-03，workflow `wf_6d8bf81a-972` 部分收口，联合门禁 0/49775 + smoke）：reviver-walk（`f96e372`）—JSON.parse reviver 每子节点降到单次 [[Get]]（=internalize_json_property 镜像）+ parse-record 树支撑 context.source（json#8/#9 收编）。message-sweep（`5decd1d`/`a5243e9`/`1643441`）—string/coercion/URI 抛点补 qjs printf 原文（"regexp not supported"/"cannot convert symbol to string"/"invalid code point"/decodeURI 具体 URIError 文本=B6 残余）。**⚠️ 编排事故**：一个 fix agent 违指示反复跑病态慢 Debug 套件（37min×3 重启）卡死 workflow，TaskStop 停止后拿已交付 patch 自集成；**promise-combinators 与 dynamic-import recon 未产出**（agent 被停时仍在调查）→ 重排。**message-sweep 残余（错误枚举映射层，sweep agent 所有权外）**：`1n/0n` RangeError 空消息(需 error.DivisionByZero→"BigInt division by zero")、`1n+1` TypeError 空、`Symbol.keyFor` TypeError 空——归入独立消息-映射切片。
- **dynamic-import 272 known ✅ 修复（`0/49775, known 304→38`）**（2026-07-03，我亲自）：根因=runner `runEmbeddedEngine` 既不装模块加载器(CLI/qjs run-test262 都装)、`ctx.eval` 又不传 filename→import() 无 callback 且 referrer 默认 "<eval>"→相对 import 相对 runner cwd 解析而 reject。**引擎 import()（D3）本已正确,runner 欠缺 host 环境**。修=装 installDynamicImport + 传测试路径作 .filename。naive 只装 callback 不传 filename=+17 回归 0 回收(referrer 仍空);加 filename 后 **fixed 267、零回归**,仅 1 条真引擎 gap 被正确度量(import-attributes/2nd-param-with-value-non-string:zjs 不 reject 非字符串属性值,qjs reject,双引擎证;此前 import 恒 reject 时"意外通过";-u 登记入其 3 个已知 import-attributes 姊妹项)。**剩余 38 known 簇**:TLA 6/import-attributes 5(D3 延迟特性)/Promise combinators ~9(promise-combinators 切片待重跑)/for-await+await+async-gen ~7(E1 余步)/harness 3/atomics.waitAsync 2/其它。
- **（旧）dynamic-import 初查悬置——已由上条解决**：CLI 装 installDynamicImport 而 runner runEmbeddedEngine 没装→import() 在 runner 下 reject；**但我亲测 naive 补 installDynamicImport 到 runner 反而 +17 回归、0 回收、单测仍 known**（specifier/referrer 解析或 async-完成检测与延迟 import job 竞争,更深),已回退。**判定悬置**:需专门调查(runner-fidelity vs 真引擎),暂列 known 主体待攻。
- **E1(1/3/4 步) ✅ + Array.fromAsync ✅ + small-items ✅**（2026-07-03，workflow `wf_6b5e7a95-32b`：4 recon 读者→蓝图 `4eefd08`→一次性实现；联合门禁 `0/49775, known 304`（440→304，-136）+ smoke + force-GC）。
  - **E1 异步生成器队列机**（`91fdea8`/`c2b3b22`）：新 `src/exec/async_generator.zig` AsyncGeneratorRequest FIFO + 6 状态机（=js_async_generator_next/resume_next/await/resolve_function/complete/completed_return 逐函数镜像）；**`.drain` eager 模式删除**——async-gen body 在每个 await 真挂起、仅经 reaction job 恢复；reentrant next 排队；AsyncFromSyncIterator 补 throw + close-wrap（=54503/54468）。46 known 消除。**E1 余步（蓝图 `E1-ASYNC-SUSPENSION-BLUEPRINT.md` 步 2/5-11）**：parser OP_await 发射（return-undefined 一 tick 残余）、**模块 TLA promise 化（5-7 步，~1000 行×3 驱动器）**、job 队列拆分/遗留删除（9-11）、§3.7 基底加固——按蓝图分步落地设计诚实整体推迟。
  - **Array.fromAsync**（`f11cc2f`，spec/test262 为参照——qjs 04be246 无此 builtin）：promise-reaction 状态机全谱（@@asyncIterator/sync 包装/array-like/mapfn await/new this()/AsyncIteratorClose 优先级），90 known 全消。集成时弃其冗余 close-on-rejection commit（E1 的 close-wrap 同物顶替），continuation tag 改编 13。
  - **small-items**：FinRec self-target 一行修复 + **顺带挖出并修预存 GC double-free**（teardown 入口补 mark=__JS_FreeValueRT 纪律，force-GC 下 SIGSEGV 复现于 clean HEAD）；nested direct eval var hoist 透传函数 env（=instantiate_hoisted_definitions 34049 + eval_caller_frame 链）。
  - **集成冲突记录**：E1 vs D3 的 runWithArgsState 31 参签名（手解一个 hunk）；E1 vs fromAsync 的 close-on-rejection 双实现（skip df437e8）与 tag 11/12 撞号（fromAsync 改 13）。
- **Oracle ✅ + Phase D ✅ 全部 10 切片完成**（2026-07-03，workflow `wf_ce1fa80d-35c`，11 agent；联合门禁 `0/49775 errors, known 440, fixed 27` + smoke 3/3 + force-GC 探针绿；47 slice commit + oracle `4cdf603` + known-errors 收缩 `396340d`）。
  - **Oracle `4cdf603`**：run_test262 强制 async 哨兵（镜像 run-test262.c js_print/eval_buf：恰一次 AsyncTestComplete，AsyncTestFailure/缺失=失败）→ **暴露 467 个 async 软失败**（8 样本双引擎人工验证全为真引擎 bug），`-u` 诚实登记。分布：dynamic-import ~288、**Array.fromAsync 90（缺 builtin，审计未单列，高价值新项）**、async-gen 簇 38、Promise 16、TLA 8、for-await/await 12、其它。快照 /tmp/zjs-async-report/。
  - **D1 for-in 重写**（=build_for_in_iterator/js_for_in_next/js_for_in_prepare_prototype_chain_enum 逐函数镜像；root-only 快照+惰性逐 proto+gopd own 重查+visited 去重；退役 simple_for_in_iterator 快路径；23 探针含 force-GC 逐字对齐；顺带修 pre-existing「迭代中 trap 抛不可捕获」）。**D2 promise**（thenable 必入队/杀 settled-adoption/await 内部 then/intrinsic ctor realm 缓存/unhandled 列表化/self-res 消息；修 11 个 known）。**D3 modules/CLI**（evaluate-once+错误缓存/import() reject 不中止/defer-to-job=js_dynamic_import JS_EnqueueJob 镜像/脚本模式 import()/JSON attributes/meta.url 绝对化/CLI 首 token 探测=JS_DetectModule+单测更新;修 16 个 known）。**D4 Date**（localtime_r 本地时区全接线+js_date_parse_otherstring 移植+ToString 强制+toLocale fmt=3+U+2212/x 映射;5 时区含 DST 边缘/半时区双引擎逐字）。**D5 TA/Atomics**（%TA% 删 9 个 Array 方法/共享 sort 参数序/Atomics detached TypeError+pre-coercion length+wait 先查阻塞/base64 GetOptionsObject/SAB grow commit-max/resize(-1)）。**D6 collections**（GetSetRecord 可观察读+负 size/groupBy 静态化/迭代器 class 查/getOrInsertComputed 重插/__setlike_mode 夹具迁出）。**D7 proxy/property**（primitive delete ToObject 序/null-base ToPropertyKey 先行/嵌套 proxy invariant 真 gopd/ownKeys 自 revoke 重查/revocable 放宽/class 字段非可扩展）。**D8 regexp 表面**（@@match/search/split 删除协议/Symbol pattern TypeError）。**D9 error/栈**（own message-only=JS_ThrowError2/AggregateError errors 非枚举/<eval> 命名/parse-error fileName+lineNumber+columnNumber+首行/行列 -1）。**D10 bigint**（全空白集/无参 TypeError/ToNumber(BigInt) 共享 coercer/1M-bit 上限修 asUintN 挂起）。
  - **判定翻转**：audit objprop#6（defineProperties 交错）前提被 D7 证伪——zjs 已是 spec 精确序且 test262 强制 → 关闭。**新 no-align**：regexp compile 子类（annexB test262 强制 TypeError）、RegExp.escape spec 表（test262 强制）、D6 GetSetRecord 前置对象检查、qjs isSealed 序。
  - **D 批诚实跳过 → backlog**：nested eval var hoist（eval_ops/call_runtime）、FinRec self-target（builtin_glue.zig:427 一行）、native 帧（需 call-machinery 帧信息）。
- **Phase C ✅ 完成（12/14）+ B7 ✅ 完成（4/4）**（2026-07-03，workflow `wf_9568aed4-edb`：5 组解析器串行链主树自闸 + B7 worktree patch 交接；联合门禁 test262 0/49775 + smoke 3/3 + force-GC 探针绿）。
  - **解析器 13 commit**（813b2d5..b3a7a1b，每条注明 js_parse_* 锚点）：no-in 泄漏（含 checkForInOfHead 重写=js_parse_skip_parens_token）、typeof/delete 标识符捷径修正、`let\n` 消歧（=is_let 28619）、`new new F().m` 结合、`delete o?.x` 真删除（OP_*_opt_chain 重写）、解构成员默认值写回（=set_val 26621）、解构参数重名矩阵（=26276/36443）、let/var 重声明 early errors（=define_var 24315）、class 体 strict 词法（=25289）、**extends 内联类不再丢字段**（=class_fields_init 25702 per-scope）、**static field super**（home=ctor，=25223/25737）、**new-spread StackMismatch**（perm3+apply1 =27359）。
  - **2 跳过（诚实，所有权外）**：import.meta 脚本模式与模块首 token 探测——根因均在 `src/cli/zjs.zig:419-425 sourceLooksLikeModule`（全流扫描 vs qjs JS_DetectModule 23792 首 token 规则）+ CLI 单测 1013-1018 固化现行为 → **移交 D3/CLI 切片**。
  - **2 条故意 spec-over-qjs 偏离（no-align 登记）**：arrow concise body 继承 no-in（test262 staging/sm 强制，qjs 04be246 自身违 spec）；`new new F().m()` 后随调用按文法/V8 解析（qjs 误当方法调用）。
  - **B7 生成器 4 commit**（adfd3d1..c86fd09）：sync finally-yield 挂起保留 pending return（=js_generator_next GEN_MAGIC_RETURN 21077/21109）；async yield await 操作数（=OP_await before OP_yield 28134）；async return(v) await 参数（=21530/28404）；gen#10 判 qjs-side bug 保 zjs+登记（test262 强制单次 inner return）。**残余（E1 队列 keystone）**：finally 队列序/悬置 return 参数/`return Promise.reject` in-try 可捕获（需 parser 发射 OP_await）/嵌套 finally 线程化/async .throw() 不恢复体（独立审计项）。
  - **⚠️ 预言机缺口（B7 发现，高优先）**：`run_test262.zig:1709-1765 runEmbeddedEngine` 不检查 async harness 软失败——`$DONE(失败)` 仅打印 `Test262:AsyncTestFailure` 不抛错，**async 软失败对门禁不可见**（这正是 gen#2/#4 坏着却 0/49775 的原因）。修复它=独立切片（可能暴露一批需 triage 的 async 失败）。B7 agent 已用严格 harness 手跑 Generator/AsyncGenerator 套件：fail 集 18→16、零新增。
- **B4 ✅ 完成**（2026-07-02，test262 0/49775 + smoke 3/3）：`toString(radix)` 重写（delta-终止最短小数+半偶舍入+进位+全 double 范围精确整数位；修小数丢失/2^128 越界/NaN 接收者；与 qjs 逐字对齐除**残余：非十进制不显著末位的最短舍入**——qjs 是 dtoa.c 多精度，300 例 fuzz 15073 字节中差 14 字节、双方均 round-trip，精确 parity 需 dtoa.c radix 引擎移植=独立切片）；round-trip band 修复（scaled_int 逆向验证，50k fuzz 0 违例）；parseInt u128 精确累加+radix10 超宽走 parseFloat（57 位审计例 bit 级对齐；非 10 radix >128bit 残余 f64）；`Number('0x…')` u128+f64 续算（不再 NaN）；toLocaleString 不再强制参数。
- **B2 ✅ 完成**（2026-07-02，test262 0/49775 + smoke 3/3，17 行差分套件与 qjs 逐字一致）：JSON.parse 退役 std.json→忠实 WTF-16 码元递归下降解析器（=js_json_parse/json_next_token 23440；lone surrogate 双向 round-trip、严格数字文法、last-wins、__proto__ own、WTF-8 intern key、Phase A 栈守卫封顶）；stringify 数字走 formatFiniteNumber=dtoa ToString（5 位点，修 1e21 记号+NoSpaceLeft 中止）；key 转义 \udXXX（json#5）；gap 10 UTF-16 单元截断（json#4）；私有字段过滤（class#3）；rawJSON ToString 强制+seal（json#6/#7）。**残余：reviver context.source 变异语义（json#8）+ 3-[[Get]] walk（json#9），中优先级**。
- **B3 ✅ 完成 `ff9e03c`**（2026-07-02，test262 0/49775 + smoke 3/3，19 项差分套件与 qjs 逐字一致）：根因=`setValueProperty` 以调用方严格性决定是否抛、数组内建传 null→静默吞。修=`setValuePropertyWithThrow(force_throw)` 变体 + 23 处接收者元素/length 写切 `setValuePropertyOrThrow`（=qjs JS_SetPropertyInt64 THROW 纪律）；reverse dense 快路径对 !extensible bail（qjs frozen 永非 fast_array）；shift/unshiftMoveIndex 改协议操作 hasValueProperty/deleteValuePropertyOrThrow（修 proxy 接收者元素不动，array#1）；删 6 处 verifyArrayLikeLengthSet 重读（array#3，qjs 不重读）。**收编 array#1/#2/#3 + objprop#1 全部**。B3 余下未动：array#5 消息（随消息批）、array#8 toReversed HasProperty 探测（low）、array#9 红线评估。
- **B3-slice ✅ 已提交**（sealed push/unshift 丢值修复：`ensureSettableForArrayBuiltin` 补「无属性无 setter + 不可扩展→NotExtensible」臂=JS_CreateProperty not_extensible 10144；NotExtensible→TypeError "object is not extensible"；unscopables 补 "at"）。**⚠️ B3 残余（下会话优先）**：sealed **splice-insert 仍静默丢值 `[1,9,null]`**（走 bypass ensureSettable 的另一写路径，需并入同一 throwing-Set 纪律）；fill/copyWithin/reverse/sort 吞写失败（array#2）、shift/unshift proxy 协议操作（array#1）、length RangeError 消息（array#5）、verifyArrayLikeLengthSet 冗余重读（array#3）均未动。
- **B6 ✅ 已提交 `ebfb439`**：① apply/Reflect.apply/Reflect.construct 参数上限 65534（`argsFromArrayLike`/`argsFromArray` 镜像 `build_arg_list` quickjs.c:41159，消息逐字）；**残余：spread `f(...arr)` 走 VM 栈路径不经 argsFromArrayLike，其上限留待 spread 调用点**。② decodeURI/decodeURIComponent 重写为码元级 `js_global_decodeURI`（quickjs.c:54755）忠实移植（`decodeUriUnits` + `uriHexDecodeAt`=hex_decode + `isUriReservedChar`），修非 ASCII+% 混合损坏/字面 `\uXXXX` 输出；22 探针逐字对齐（含 malformed UTF-8 拒绝表）。**残余：malformed UTF-8 的消息文本仍是全局 InvalidUtf8→"expecting hex digit" 映射（应为 "malformed UTF-8"），随系统性消息批清理**。encode/escape/unescape 面复验未动、与 qjs 一致。

- **Phase A ✅ 已实现（分支 `qjs-align-phaseA`，未提交）** 2026-07-02。test262 **0/49775**（与基线一致，A3 可观察改动零回归）。
  落地：InternalError native 错误类（registry.zig 6 处 switch + error_names.zig，与其余 6 个 native error 同构，`typeof/​.name/​.message/​proto 链/​.length` 全对）；`JSRuntime.native_stack_top/limit/size` + `updateNativeStackTop`（=qjs `JS_UpdateStackTop`，在 `eval()` 外层 `call_depth==0` 刷新，跨线程正确）+ `checkNativeStackOverflow`（=qjs `js_check_stack_overflow` 逐字：`sp=@frameAddress()-alloca; sp<limit`，1 MiB 预算=qjs `JS_DEFAULT_STACK_SIZE`）；`error.StackOverflow`→InternalError "stack overflow" 映射（runtimeErrorInfo/promiseErrorInfo/errorNameForRuntimeError）；`throwInternalErrorMessage`/`throwStackOverflow`。守卫接线（均双引擎实测对齐 qjs）：parser `advance`（深嵌套→catchable SyntaxError）、JSON.parse `SimpleJsonParser.parseValue`（→SyntaxError）、JSON.stringify 三路径 `qjsJsonAppendSimpleValue`/`qjsJsonSerializeProperty`/`appendJsonValue`（→InternalError "stack overflow"）、`Array.isArray` proxy 链改**有界循环** depth>1000（=qjs `js_resolve_proxy`）、`qjsArrayJoinCall`（环形 join/toString→InternalError）、JS 调用深度 3 处 RangeError→InternalError "stack overflow"（=qjs `JS_ThrowStackOverflow`）。覆盖审计条目：array#0、json#1/#2、proxy#9、error#2/functions#3（InternalError 类）、string 侧未含（见 B1）、parser 深嵌套（新发现）。
  - **预算实证**：主线程 `zjs -e` 在 `eval()` 帧以下仅约 2.5 MiB 可用（CLI 调用链已耗栈），故 1 MiB 预算在 ~5000–10000 native 层触发、远低于真实崩溃、且远高于任何合法嵌套；worker 线程 16 MiB 更宽。4 MiB 曾因主线程 headroom 不足而在守卫前先崩，已降至 1 MiB。
  - **⚠️ 新发现（不在原审计 4 崩溃点内，独立项）**：**深层嵌套结构的 free/GC 遍历原生递归崩溃**——构建 120000 层嵌套数组即 SIGSEGV（80000 OK），qjs 迭代式 decref 可survive 200000。根因=zjs 对象 free/decref 路径递归（object.zig free 链），非崩溃点守卫可解，需**改为迭代 worklist**（=qjs `free_zero_refcount`/`gc_decref`）。**归入 Phase E（GC 迭代化）**。这也是 json#2 repro 在 200000 仍崩的原因（stringify 守卫本身已对；崩在 build/teardown）。

## 2. Phase A — 基础设施（0.5–1 天）✅ 已实现

| # | 内容 | qjs 锚点 |
|---|---|---|
| A1 | 原生栈守卫：`Runtime.stack_top` 于创建时捕获（`js_get_stack_pointer` 等价 `@frameAddress`）、顶层 eval 入口刷新；`checkStackOverflow(rt, alloca_size)` helper；`stack_size` 默认 1MB | quickjs.c:2043-2062、2858；quickjs.h:328 `JS_DEFAULT_STACK_SIZE (1024*1024)` |
| A2 | `InternalError` 原生错误类（构造器上 global、原型链、name/message）+ `throwInternalErrorMessage`；`JS_ThrowStackOverflow` 等价（`InternalError "stack overflow"`） | quickjs.c:7789-7791；native error 表 |
| A3 | 现有 JS 调用深度 `RangeError('Maximum call stack size exceeded')`（inline_calls.zig:270、vm_call.zig:77/88）→ `InternalError 'stack overflow'`。**红线规则**：全量 test262 若红则保留 RangeError、登记 | quickjs.c:17581 等 |
| A4 | 守卫接线：**解析器递归**（已实测 50k 层括号 SIGSEGV，qjs 干净 SyntaxError "stack overflow"）、array join/toString 环、JSON parse/stringify、`Array.isArray` 深 proxy 链 | quickjs.c:22836/23484（parser）、js_json 顶部、JS_IsArray |

覆盖审计条目：array#0、json#1、json#2、proxy#9、error#2/functions#3（类本身）＋新发现解析器崩溃。
验收：全部崩溃探针变可捕获异常；全量门禁。

## 3. Phase B — 崩溃与数据丢失（红线批）

按文件所有权切片，互不冲突，可并行实现、逐个集成。

### B1 string 长度上限（0.5–1d）｜`core/string.zig`、`builtins/string.zig`、`exec/string_ops.zig`
- `JS_STRING_LEN_MAX = (1<<30)-1` 于创建/concat/rope 合并处（热路径仅一次整型比较）；
  `repeat`：`count > 2^31-1` → RangeError `invalid repeat count`、`count*len > MAX` → RangeError。
- 条目：string#0、string#1。⚠️ perf 敏感（`bf98cf3` 恢复的 rope 快路径）→ 附 `s=s+"a"`/churn
  微基准前后对比，不得回退。

### B2 JSON 忠实重建（2–3d）｜`core/json.zig`、`builtins/json.zig`（**禁碰 parser.zig**）
- **推荐路线：JSON.parse 整体重写为镜像 `js_json_parse` + `internalize_json_property`**
  （自带 WTF-16 tokenizer），一次收编：深度守卫（A4）、lone surrogate（json#3/#5）、
  reviver source 语义（json#8）、reviver 单次 [[Get]]（json#9）、std.json 后端退役（json#10）。
  备选：逐条补丁（风险分散但总量更大、留结构债）。
- stringify 侧：数字走引擎 dtoa 的 ToString 路径（json#0 + number JSON 条，同根修复）；
  gap 多字符非 ASCII 修复 + 10 UTF-16 单元截断（json#4，复核修正版）；key 的 lone surrogate
  `\udXXX` 转义（json#5）；`rawJSON` freeze + ToString（json#6/#7）；
  **私有字段过滤**（class#3，simple-object 快路径滤 private atom）。
- 条目：json#0–#10、class#3。门禁另加 JSON 目录 test262 + 双引擎大 corpus 差分。

### B3 数组封印/写失败簇（1–1.5d）｜`builtins/array.zig`、`exec/array_ops.zig`（**不碰共享 sort**）
- push/unshift/splice 增长路径走 CreateDataPropertyOrThrow 语义：`!extensible` → TypeError
  `object is not extensible`（objprop#1）；
- fill/copyWithin/reverse/sort/shift/splice 写失败必抛（sloppy 同样，qjs `JS_PROP_THROW`）（array#2）；
- shift/unshift 元素搬移用 [[HasProperty]]/[[Get]]/[[Set]]/[[Delete]] 协议操作（proxy 正确）（array#1）；
- `length` 无效值 RangeError 可捕获 + `invalid array length` 消息（array#5）；
- pop/push 删除 `verifyArrayLikeLengthSet` 多余重读校验（array#3）；
- `Symbol.unscopables` 加 `at`（array#7）；toReversed 补 [[HasProperty]] 探测（array#8，低）。
- 红线规则条目：array#9（≥2^32 array-like 迭代，qjs uint32 截断 vs spec ToLength）→ 先查
  test262 覆盖，冲突则 no-align 登记。

### B4 number 格式化簇（1.5–2d）｜`builtins/number.zig`、`core/value_format.zig`、`core/number.zig`、`builtins/math.zig`
- `toString(radix)` 移植 qjs `js_dtoa` radix 自由格式（小数位 + shortest-roundtrip + 消 2^128 越界）（num#2）;
- `String(double)` round-trip band：`formatSimpleFiniteDecimal` 加精确性验证或删快路径走 dtoa（num#1）；
- `parseInt` >19 位切 bigint 累加 + 正确舍入（num#4）；`Number('0x…')` u64 溢出后 f64 继续累加（num#5）；
- `Math.random` = xorshift64star over `rt.random_state`（镜像 `js_math_random`）（num#0）；
- `toLocaleString` 不强制参数（num#7）。
- 条目：number-math-dtoa#0–#5、#7。libm 末位 parity（num#6）→ no-align 登记（见 §7）。

### B5 →（并入 Phase C：extends-inline-class 字段丢失、super-in-static-field 均为解析器/发射层）

### B6 URI + 调用上限（0.5d）｜`builtins/uri.zig`、`exec/call_runtime.zig` 参数物化点
- decodeURI/Component 重写为 qjs 码点级算法（latin1 0x80-FF、astral 正确；错误消息表对齐）（functions#0/#6 部分）；
- apply/Reflect.apply/spread 参数物化 65535 上限 → RangeError `too many arguments…`（镜像
  `build_arg_list`）（functions#1）。

### B7 生成器有界簇（1–2d）｜`exec/vm_gen_async.zig`、`builtins/iterator.zig`
- sync `gen.return(v)` 在 finally-yield 挂起时的悬挂完成丢失（gen#3，纯 sync 机制，先修）；
- async-gen `yield` await 其操作数、`.return(v)` await 参数（gen#2/#4）——**仅做不依赖队列重写的部分**；
  残余（时序、重入排队）显式留给 E1 并在 catalog 标注边界；
- Iterator.prototype.flatMap `.return()` 内迭代器双关闭顺序（gen#10，低）。

## 4. Phase C — 解析器批（3–5d）｜单一所有权 `parser.zig` + `bytecode.zig`

单 agent 串行完成（同文件禁并行），每条独立 commit + 定向 test262（`language/` 相关目录），
批末全量门禁。优先级序：

| # | 条目 | 内容 |
|---|---|---|
| C1 | parser#0 | for-init `no-in` 限制泄漏进括号/嵌套子表达式（改为随括号层复位的状态） |
| C2 | parser#3 | `let\n<ident>/{…}/[…]` 跨行误判为表达式（lookahead 修正，ASI 规则对齐） |
| C3 | parser#2 | `new new F().m` 成员尾结合错误（member-expression 归属内层 new） |
| C4 | parser#6 | 解构赋值默认值到成员目标 `({y:o.e=5}={})` 不赋值（cover-grammar → 赋值路径） |
| C5 | class#0 | **extends 内联类表达式 → 实例字段初始化器全丢**（class-in-class 解析状态覆写） |
| C6 | parser#1 | 含解构/rest/重命名参数时的重名参数 early error |
| C7 | gen#5 | `new f(...spread)` in try → StackMismatch（字节码栈平衡账目） |
| C8 | parser#5 | `delete o?.x` 静默 no-op → 真删除语义 |
| C9 | parser#7 | let/var、const/var、function/let、class/var 冗余声明 early errors |
| C10 | class#1 | static field initializer 中 super 属性访问（home object=constructor） |
| C11 | parser#4 | class 方法/字段体 strict 词法（八进制/`\8` 拒绝） |
| C12 | parser#8 | `typeof async function(){}` 等关键字后函数表达式误拒 |
| C13 | parser#9/#10 | `import.meta` 脚本模式 SyntaxError；late-export 模块自动检测（低，对齐 qjs 首 token 规则须先查 test262） |

⚠️ 每条动 ASI/cover-grammar 都可能牵连：C 批内每条完成即跑 `language/` 全目录，批末全量。

## 5. Phase D — 行为对齐批（9 切片，文件所有权互斥，可并行）

| 切片 | 文件所有权 | 条目 | 要点 | 估时 |
|---|---|---|---|---|
| **D1 for-in 重写** | `exec/forof_ops.zig`、`exec/iterator_ops.zig` | objprop#0、proxy#0/#4、gen#6/#12 | 镜像 `build_for_in_iterator`/`js_for_in_next`：仅快照 root own keys；原型链惰性逐层 + visited 去重；每步 own-check（gopd/proxy-gopd）替 HasProperty | 1–2d |
| **D2 promise 忠实化** | `core/promise.zig`、`builtins/promise.zig`、`core/jobs.zig` | prom#1/#2/#3/#4/#5/#8 | thenable 必入 job；native promise 采纳走 then-job（+2 ticks）；await 走内部 perform_promise_then（不读可变 `then`）；intrinsic ctor 缓存（不读 globalThis.Promise）；unhandled-rejection 列表化；self-resolution 消息。**先固化 33 条时序探针再动代码** | 2–3d |
| **D3 模块/eval** | `core/module.zig`、`exec/module_graph.zig`、`exec/eval_entry.zig`、`runtime/`、`cli/` | mod#0/#1/#2/#3/#6/#7(import()脚本模式)/#8(attributes)/#9(meta.url)/#11、modules#6(嵌套 eval var) | evaluate-once + 错误缓存重抛；import() 失败→reject（ReferenceError）；import() 延迟到 job；模块模式退出前 flush stdout；`-e`/脚本模式支持 import()；JSON import attributes；meta.url 绝对 file://；嵌套 direct eval var hoist；消息不泄内部函数名 | 2–3d |
| **D4 Date 重建** | `builtins/date.zig`（+小 tz helper） | date#0–#6、bigint#2 的 Date 站点 | **本地时区**：libc `localtime_r`（build 已 `link_libc=true` ✅）镜像 `js__date_getTimezoneOffset`，接入 toString/getHours/setters/ctor(y,m,d)/ISO-无-TZ；`js_date_parse_otherstring` 宽松解析移植（月名/斜杠/tz 缩写/AM-PM）；parse 走 ToString 强制；toLocale* fmt=3；U+2212 映射；`'integer'` hint（红线规则）。**门禁在宿主 TZ（Asia/Shanghai）跑全量 test262** | 3–5d |
| **D5 TypedArray/Atomics** | `core/typed_array.zig`、`builtins/buffer.zig`、`builtins/atomics.zig`、`exec/atomics_wait.zig`、共享 sort helper | ta#0/#1/#2/#3/#4/#5/#7/#8；红线评估 #9/#10/#11 | 删 %TA%.prototype 9 个 Array-only 方法（spec 违反+非 qjs）；**共享 sort 比较器参数序 (a,b)**（同修 Array）；Atomics：detached→TypeError、索引对 pre-coercion length 验证、wait 先查可阻塞；base64/hex GetOptionsObject；SAB grow 预留 max 提交式；resize(-1)→TypeError。#9/#10/#11（qjs-vs-spec）跑定向 test262 决定 align/no-align | 1.5–2d |
| **D6 collections** | `core/collection.zig`、`builtins/collection.zig`、`src/tests/builtins.zig` | coll#0/#1/#2/#4/#5/#6/#7 | GetSetRecord 可观察 has/keys 读 + size<0 RangeError；Map.groupBy 去接收者绑定；Map/Set 迭代器 class 校验；getOrInsertComputed 删后重插尾；FinRec register(target==registry)；**`__setlike_mode` ~500 行夹具迁出引擎**（单测改用真 set-like 对象，依赖面已查：仅 `src/tests/builtins.zig`） | 1d |
| **D7 proxy/property 杂项** | `builtins/reflect_proxy.zig`、`exec/vm_property*.zig`、`builtins/object.zig`、`exec/class_init_ops.zig`、`builtins/function.zig` | proxy#1/#2/#5/#6、objprop#2/#3/#5/#6、array#6、class#2、functions#2/#7 | 嵌套 proxy invariant 用真 [[GetOwnProperty]]/[[IsExtensible]]；ownKeys 自 revoke 重查；revocable 接收者放宽；proxy-fn/bound-fn `toString` 名字（`bound foo`、经 get trap 读 name）；bound 属性序 length,name；primitive base delete（ToObject+attrs+strict TypeError）；null base 先 ToPropertyKey；defineProperties/create 先批量 gopd（spec=qjs 一致）；isSealed/isFrozen 顺序（红线规则）；**typeof-in-with**（with-scope 绑定参与 typeof 解析）；class 字段走 CreateDataPropertyOrThrow | 1.5–2d |
| **D8 regexp 表面** | `builtins/regexp.zig`、`core/regexp.zig` | re#0/#1/#2/#3 | Symbol.match/search/split 被删后走通用路径（match/search TypeError、split 走字符串分隔）；compile 放宽到子类实例；Symbol pattern/flags → ToString TypeError；RegExp.escape 补 \xHH/控制字符表 | 0.5–1d |
| **D9 error/栈** | `builtins/error.zig`、`core/exception.zig`、`exec/error_stack_ops.zig` | err#1/#2余/#4/#5/#6/#8/#11 | own `name` 改原型读取（红线规则）；OOM/interrupted 归 InternalError；backtrace 补 `(native)` 帧；顶层帧名 `<eval>`；parse-error 挂 fileName/lineNumber/columnNumber + 首行 at；AggregateError 不定义 own message、errors 不可枚举；行列 -1 调整 | 1–1.5d |
| **D10 bigint** | `core/bigint.zig`、`builtins/bigint.zig`、`exec/coercion_ops.zig` | bi#0/#1/#2非date/#3 | StringToBigInt 全 spec 空白集；`BigInt()` 无参 TypeError；非 date ToNumber(BigInt) TypeError 站点；`JS_BIGINT_MAX_SIZE`=1M bits RangeError（顺带修 asUintN(2^32) 挂起） | 0.5–1d |

## 6. Phase E — 深前沿 keystone（各自独立立项，一次写到终态，蓝图先行）

| # | 内容 | 收编条目 | 规模 |
|---|---|---|---|
| E1 | **异步挂起模型重写**：AsyncGeneratorRequest 队列、await 真挂起（禁同步 drain job 队列）、TLA 真挂起与兄弟模块交错 | gen#0/#1、prom#0、mod#4、B7 残余 | 1–2 周，最大正确性 keystone；依赖 D2 先落（忠实微任务原语）|

**E1 状态（2026-07-03，async-generator 半程落地，蓝图 §4 步骤 1/3/4 完成）**：
- ✅ **AsyncGeneratorRequest 队列 + 6 状态机落地**（`src/exec/async_generator.zig`，镜像
  js_async_generator_next/resume_next/await/resolve_function/complete/completed_return
  quickjs.c:21706/21568/21446/21670/21520/21532）；**.drain 模式删除**——async gen body 的
  await 真挂起（.raw），恢复只经 promise-reaction job；重入 next() 只 append（EXECUTING 不再 TypeError）；
  COMPLETED 态每次 resume_next 只 settle 一个请求 + AWAITING_RETURN settle 后不续 drain
  （双双实测 qjs 二进制坐实的 qjs-vs-spec 偏离，逐字镜像）。
- ✅ **AsyncFromSyncIterator throw + close-wrap onRejected**（quickjs.c:54503-54520/54468-54476）；
  for-await await 窗口 unwind 不再关迭代器（js_for_await_of_next 禁 catch offset 的镜像，
  close-wrap 是唯一 closer）；OP_throw_error type 4 带 qjs 消息。
- **Parser 残余**（本切片 parser.zig 禁区，以 trampoline action 驱动侧承载）：
  yield 操作数 await（OP_await before OP_yield 28134）= `.yield_operand`；
  显式 return 值 await（emit_return 28402，hasval 才 await）= `.complete_result`/`.return_arg`
  （以值非 undefined 代理 hasval → 显式 `return undefined` 比 qjs 早一个 tick）。
- 门禁：test262 **0/49775**（known 440→394，**fixed 46**：AsyncFromSync 12 + AsyncGeneratorPrototype 15 +
  async-gen 语言形态 15 + await 2 + await-using/using 3）；smoke 3/3；22 探针双引擎逐字一致（普通 + force-GC 构建）。
- ⏳ **未落（蓝图 §4 步骤 2/5–11）**：模块 TLA promise 化（.settled 模式仍在；ModuleContinuation
  重编译驱动仍在）；executePendingJob/hostRunJobs 拆分；legacy rt.job_queue 删除；
  Atomics.waitAsync re-plumb；parser 发射 OP_await（步骤 2，收编上面两个残余）。
| E2 | 模块管线重写（编译一次、记录驱动，qjs `js_module_*` 形态；消每模块 3+ 次重编译） | mod#10 | ~1 周 |
| E3 | Map/Set 墓碑 → qjs 记录级 free + hash 重建（O(1) churn） | coll#3 | 2–3d |
| E4 | 私有 brand 机制 → 原型/brand 检查（弃逐实例拷贝 private props） | class#4 | 3–5d |
| E5 | 生成器 return/throw 完成模型 → qjs 形态（弃 finally 字节码扫描） | gen#11 | 3–5d |
| E8 | **对象 free/decref 迭代化**（弃递归 free，用 worklist=qjs `free_zero_refcount`/`gc_decref`）：修深层嵌套结构 build/teardown SIGSEGV（120000 层崩，qjs survive 200000）。Phase A 新发现。 | 新 | 3–5d |
| E6 | （视 B2 路线）JSON.parse 若未整体重写则此处收尾 | json#10 | — |
| E7 | （可选）Array.sort → rqsort 结构对齐 | array#4 | 默认不做，见 §7 |

## 7. no-align 登记（写入 DIVERGENCE-CATALOG，附 test262/复核证据）

1. **qjs 上游 bug（zjs 正确，已双引擎坐实）**：bigint `x--` 加 2^32-1；`asUintN(64,-1n)`→`-1n`；
   mixed bigint↔double 关系比较丢符号；`Math.sumPrecise` 有限负和→`+Infinity`。候选上游报告。
2. proxy#7：ownKeys trap 返回非对象——spec/test262 要求 TypeError，qjs 宽容读 length。保持 zjs。
3. objprop#7：`JS_CreateProperty` 数组 length 先更新后验证——qjs 自标 `XXX` 的怪癖。保持 zjs。
4. num#6：超越函数 libm 末位 parity（pow ~76% 末位差）——spec 允许近似实现，zjs 部分场景更准。
   接受偏离；若未来需要 digit-parity 再评估链接系统 libm。
5. ta#9/#10/#11、date#5（`'integer'` hint）、array#9、isSealed/isFrozen 顺序（objprop#5）、
   defineProperties 交错序（objprop#6 已按 spec=qjs 修，无冲突）：**逐条跑定向 test262 后定夺**，
   spec 与 qjs 冲突且 test262 覆盖者一律保 zjs + 登记。
6. PTC 两条（error#9/#10 KNOWN）：LIMITATIONS.md 已记录，维持。

## 8. Phase P — 策略决策点（用户拍板；默认推荐已给，不阻塞 A–D）

| # | 项 | 推荐 |
|---|---|---|
| P1 | V8 栈簇（captureStackTrace/prepareStackTrace/CallSite/stackTraceLimit=10 截断） | 引擎默认对齐 qjs（stack=eager own data property、不截断）；V8 簇整体移 host/runtime opt-in 层（fun 保留能力）。最小改：先把 stackTraceLimit 默认改 ∞ 消截断 |
| P2 | 非 qjs 全局（atob/btoa/queueMicrotask/navigator/DOMException/gc/TypedArray/DisposableStack…） | 移到 CLI/runtime 注入层：裸引擎 globalThis = qjs 面；zjs CLI/fun 注入保持现状 |
| P3 | Promise.allKeyed/allSettledKeyed、Iterator.zip/zipKeyed、generator `slice` | 查 fun 使用面后移 host 层或删；引擎不保留 |
| P4 | Immutable ArrayBuffer 提案面（sliceToImmutable/transferToImmutable/immutable） | 保留为有意超集并登记（test262 staging 有覆盖）；或 flag 化 |
| P5 | print/console.log 格式（qjs JS_PrintValue inspector、`10n`、`-0`、Map 展开…） | 对齐 qjs（差分测试受益）；先确认 fun 快照/固定输出依赖 |

## 9. 风险登记

- **D2 promise 时序**：全局可观察面。先固化探针 corpus（P1–P33 双引擎期望），改后逐条对照 + 全量 test262。
- **D4 时区**：test262 必须在宿主非 UTC TZ 全绿（qjs 能）；DST 历史用 glibc tzdata（与 qjs 同源，天然一致）。
- **B3 sloppy 抛错扩大**：真实代码兼容风险，但 qjs/spec/test262 三方一致，风险可担。
- **B1 热路径**：长度检查必须是单次比较；微基准回归即调整放置点。
- **C 批**：ASI/cover-grammar 连锁——逐条跑 language/ 目录。
- **D1 for-in**：枚举核心 + 迭代器持引用 → force-GC 必跑。
- **D6 夹具移除**：先改写 `src/tests/builtins.zig` 相关用例再删引擎路径，同一 commit。
- **消息文本**：批量补消息时逐条对照 qjs printf 原文，不自创措辞（差分探针会抓）。

## 10. 工作量汇总

- A：0.5–1d；B：6–9.5d；C：3–5d；D：14–20d → **有界部分合计 ~24–35 人日**（含门禁）。
- E：另计 ~4–6 周（E1 最大，蓝图先行逐项立项）。
- 系统性空消息全面清扫（err#7/string#4/coll#8/json#11/bi#4/gen#9/class#5/re#4/objprop#4…）：
  随各切片顺带修其触碰站点；**全面清扫**作为独立尾批（fan-out 按 builtins 文件分片 + 差分探针验收），~3–5d，排 D 后。
