# TODO: 评估年轻代 nursery

Status: TODO — 待评估，尚未确定具体方案与默认启用策略

所属讨论：[JSRuntime Review M14](runtime-review/history.md#内存能力已讨论的边界)。

2026-09-21：用户要求将 nursery 评估记录为待办。本次只记录，不实施改造。

## 评估范围

- 比较当前复制式 nursery 与基线回收路径的吞吐、GC 暂停、峰值及稳态内存。
- 覆盖大量短命对象、高存活率、长期运行及跨代引用密集的负载。
- 核算写屏障、存活对象复制与晋升、保守根导致的 pin 和整页保留成本。
- 查明当前默认配置、实际进入的分配/回收路径及对象覆盖范围，避免只凭模块存在推断启用状态。
- 根据证据决定是否采用当前实现、默认启用，以及容量和晋升策略；参数不预先定案。

## 参考与边界

参考 V8 分代与 Scavenger 的设计说明：
https://v8.dev/blog/trash-talk#generational-layout
该文章发布于 2019 年，用于机制参考，不代表当前 V8 所有配置。

zjs 实现入口：`src/core/gc_nursery.zig`、`src/core/gc.zig`。
分代方向的讨论不等于已批准当前复制实现或参数；测量遵循现行验证政策。
