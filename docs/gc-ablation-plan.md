# GC 代码量消融计划（勘察综合，driver 审阅中）

Status: **本批已收官（2026-09-03，全部合入 main）；余项转 TGC S5 消融规格
[`tracing-gc-s5-spec.md`](tracing-gc-s5-spec.md)（S5-a/b/c/d 四批），见下节的对照表。**
本文继续作为条目账本与勘察原文；新增消融条目请写进 S5 规格，不要在此表新增。

原状态：**EXECUTING（分支 `gc/ablation-20260903`，worktree `/home/aneryu/worktrees/gc-ablation`）— 2026-09-03 owner「并行消融缩减 GC 代码量」；七组只读勘察 + 逐组对抗核验 + 综合排序（workflow wf_7c269289）；driver 逐批亲核亲改，每批过 `zig build test` + `test-gc-stress` + test262 script + `mise run stage0`。**

执行记录（每批一个 commit，门禁读数在 commit message）：

| 批 | 条目 | 净减行 | 备注 |
|---|---|---|---|
| 1 `97f2d1ee` | A01 slab locality audit、A02 gc_slot/litmus/write_audit 脚手架（46 处 `auditWrite` 双臂折叠）、A04 collectConcurrentMajor、A06/A07 registry 诊断解析与 string/rope kind、A15、A16、C05、C06 | −1,960 | `zero ref drains` token 保留为字面 0（gc_stats_snapshot.py 解析） |
| 1c `90b70d1d` | gc.zig 零调用者 helper 束、Policy Mode/forMode/slice budgets、memory.zig 死 API、块堆死项、conservative AddressLookup、parallel_threshold | −273 | Stats/Heap 尺寸 pin 同步 |
| 2a `12f4f52b` | B02 sticky major 实验整体（build 选项、三个 env 旋钮、scope/状态/统计/调度臂/四个测试） | −617 | heap-accounting oracle 独立成 Registry 字段；`ZJS_GC_VERIFY_MAJOR_ALL` 留给 roots-diag |
| 2b `2d157e71` | B03 并行标记（gc_parallel_mark.zig、admission、tryAcquireHeaderMark、CLI 行、六测试） | −761 | `gc_mark_pool` 改为 `IncrementalMarkState`（footprint + settled-live）；stage0 首跑 splay「deferred block runs」−10.8% 复跑 −2%，该计数随 wall-clock 切片走 |
| 3a `9ac12f08` | D03 mark queue 单线程折叠（两把 pthread mutex、原子 failure、测试计数器、locked push/pop 双胞胎、donate、Block.tryAcquireMark） | −213 | `Queue.steal` 保留（barrier 段整段进私有栈） |
| 3b `3338c21d` | D01 sweep model（模块、Registry 字段、begin/end/verify 钩子、drained_sweep_debt、11 个 Report 字段、root 导出、三测试） | −431 | `Block.sweep_state` 的枚举迁到 gc_block_heap.SweepState |
| 3c `694e82cb` | B01-lite carrier authority 选择层 + A05 Report 收缩（12 个只写字段、format、SurvivorClass、`--gc-stats` 下的全堆 liveCount/对象计数普查） | −428 | B01-lite 纯 comptime 删除却重排 ReleaseFast 布局，配对 ABBA×4 读到 regexp cycles +2.7%/insn +0.02%。**owner 裁决（2026-09-03）：布局重排导致的劣化先忽略**——消融批次只按 insn 与功能门禁判，cycles-only STOP 记账不阻塞 |
| 3d `d9a9e0ed` | C01 未被 tools/perf 解析的 `--gc-stats` 行（块堆拓扑/cell 容量/逐 class、Object publications、slots2 attaches、mark frontier）+ C04 destroy probe 及 `ZJS_GC_DESTROY_PROBE` | −112 | 计数器本体（block census、slots2 计数）保留给测试 |
| 3e `3c31a5d6` | C18 五个文件头的 rc 时代叙述、root.zig 三个恒真 `if (X_enabled) @import else struct{}` 桩 | −25 | 纯注释/桩 |
| 3f `673ac063` | C02 frontier/carrier 注入变异旋钮（`ZJS_GC_FRONTIER_INJECT`/`ZJS_GC_CARRIER_INJECT` 与三处变异臂；`m_cut_inject` 因测试仍读而保留）+ C03 阈值三个环境覆盖（`ZJS_GC_MIN_THRESHOLD`/`ZJS_GC_HEADROOM`/`ZJS_GC_GROWTH_PERCENT`）+ E01 destroyZeroRefNow 内联 | −170 | runtime.zig 阈值计算少三个运行时分支 |

累计 **−4,990 行**（分支对 S0 合入点：−4,929/+548；gc*.zig+object_gc+memory 由 19,120 行降到约 16,300）。

### 余项对照 TGC S5 规格（更新 2026-09-06）

下表把本批「待裁决」条目逐条对到 [`tracing-gc-s5-spec.md`](tracing-gc-s5-spec.md) §2 的四批。
S5 的目标是净删行与可维护性，不是性能；每批只做 Stage 0 快筛（insn 口径）确认无回归。

| 原条目 | 估算 | 归宿 | 说明 |
|---|---|---|---|
| D02 恒真 comptime 门全树折叠 | ≈250 | **S5-a 第 1 项** | 删 `-Dzjs_gc` 选择器与 `build_options.zjs_gc`；`trace_stw_enabled` / `block_heap_enabled` / `generation_enabled` / `address_registry_enabled` / `space_model_enabled` / `concurrent_enabled` 六常量删除，~190 处 `if (comptime …)` 展开；`gc_representation` 快照重生成 |
| D07 两套 major 统一 | ≈300 | **S5-b 第 1/4 项** | `destroyCondemned` ⊕ `destroyDoomedSlice` → 一个 `destroyCondemnedSlice(budget_ns, clock_cadence, sweep_extents)`（STW = `budget = maxInt`）；`collectCycles` 若能收缩成「begin → 全量 markStep → finish」则 STW 专用函数体整体删除 |
| C11-C16 诊断族 | ≈905 | **部分进 S5-a 第 3 项** | S5-a 只处理 `--gc-stats` 的**过期行**（删 `zero-ref drains` / `refcount-removed headers`、`pass-A settled cells` 改名 `bitmap reclaimed cells`、删 `parked_frees 0`）并配 `SCHEMA_VERSION 9` + `SCHEMA_REMOVED_LEAVES` 映射；计数器本体仍被 Stage 0 工具解析，删除仍需裁决 |
| D05 `gc.Stats` 死字段与 `GeStats` 合并 | ≈150 | 仍待裁决 | 是 embedding API（`JSRuntime.gcStats()`），未进 S5 |
| D10 memory.zig 分配器分层 | ≈150-250 | 仍待裁决 | 最热分配路径，布局效应大，未进 S5 |
| B04 RSS/cgroup/external 压力策略 | ≈130 | 仍待裁决 | embedding 选项，删即 API 变更 |
| B05 `-Dzjs_oom_coverage` | ≈60 | 仍待裁决 | 非 GC 专属 |
| D04 RefKind 描述符目录 | ≈100 | 仍待裁决 | 要重生成 representation snapshot 基线；注意 `RefKind` 已由 8 值扩到 13 值（S4-a/S4-c/S2-i），估算需重做 |

S5 在本表之外新增的条目（详见规格 §2）：

- **S5-a**：rc 命名整改（`metadata_rc_offset` → `metadata_lifetime_offset`，poison 注释与 `@compileError` 文案）；`MarkFootprint.refcount_removed_headers` 字段删除；**修 atom audit 行的解析正则**——解析器找的是 `missing-edge/over-marked` 而面板打的是 `stale-edge/shell-edge`，永不匹配静默回 0；13 个 `pub fn` 降私有、3 个 test-only 加门。
- **S5-b**：三处凝判扫链（`sweepUnmarked` / `sweepUnmarkedYoung` / `finishIncrementalCycle` 内联段）合成 `condemnListSweep`；增量 `markStep` 的手写 frontier 循环改用带预算的 `drain`；STW 入口双 `beginMajor` 去重。
- **S5-c**：`concurrent` → `incremental` 改名（`gc_concurrent.zig` 零线程，`major_marking_active` 的 atomic 与单线程注释矛盾）；`IncrementalMarkState`（`gc_mark_pool`）拆解；**文档 supersede / historical / 活文档改写**（本次提交）。
- **S5-d（owner 可选）**：`Registry` 3,796 行 / 37 字段 / 114 方法拆成七个子结构，硬约束是 `phase align(64)` 必须在 offset 0 且与 `barrier_gate` 相邻。

**计划外删除的条目**：完成计划 S5 原有的「删 `gc_conservative` 生产路径的门」以 R1 全精确根为前提；
owner 2026-09-06 裁「R1 不立项，S5 消融后转 TS/AOT」，故该项取消，**保守扫描保持生产默认**
（`tracing-gc-s5-spec.md` §0）。

不需裁决但收益已很小（≤50 行/条）：D06 NonBlockObjectAuthority 转发器、D09 wrapper/alias 束、C07 registry 测试镜像计数、C08 验证器精简、C09 `AllocInfo.large`、C10 尺寸 pin、C17 pause ring 缩容。
其中 C09 当初「S4 会重排 flags，顺延」的理由已兑现：S4-e/S4-h 已把 flags 字节定死为
`kind:u4 | young | finalizing | needs_finalizer | reserved`，`lifetime.flags`（byte 7）整字节空闲。
Stage 0 的「splay deferred block runs」计数呈双峰（1691 或 1509），已多次与 insn 持平同时出现，视为 wall-clock 切片耦合，不作阻塞。
门禁链已改并行形式（一张构建 + stage0‖test262），每批约 7.5 分钟。stage0 记账：splay insn 0.9915（B02 后）～1.0025，其余负载 0.997-1.000。

> 下文为综合代理原文（英文）。行号对应 gc/tgc-s0-20260903 工作树在勘察时刻的状态；执行前按符号名重新定位。

# GC ablation — ranked execution table (synthesis of 7 verified surveys, branch `gc/tgc-s0-20260903`)

Working-tree sizes re-measured now: gc.zig 6296, gc_trace_stw.zig **3010** (+16 vs brief; refs after ~1577 are +8, after ~2886 +16), gc_block_heap.zig **2699**, memory.zig 2244, gc_address_registry 892, gc_conservative 634, gc_mark_queue 557, gc_parallel_mark 545, gc_carrier 468, gc_generation 417, gc_space 256, gc_slot 232, object_gc 209, gc_concurrent 201, gc_sweep_model 200, gc_write_audit 129, gc_slot_litmus 111, gc_representation_constants 52; plus **slab_locality_audit.zig 729** (outside the 16.9k count, comptime-false). src/tests/core.zig refs ≥16000 are −2 vs the surveys.

## 0. Before starting (blockers, not ablation)

| # | item | evidence | action |
|---|---|---|---|
| 0.1 | **`mise run stage0` is broken on this tree** | uncommitted `src/cli/zjs.zig:1095` now prints `finish-init {d}` / `finish-tail {d}` in the `gc: incremental subphase ns totals` row; `tools/perf/gc_stats_snapshot.py:297` still requires the 8-field row (`one_match` → `SnapshotError`); fixture `tools/perf/verify/test_gc_stats_snapshot.py:34` also old. Verified again just now. | update regex + fixture (or move the two fields to the unparsed reconciliation row) **before any batch**, since stage0 is the gate after every batch |
| 0.2 | in-flight S0 edits are excluded | `Block.forgetDoomedCell` (gc_block_heap.zig 520-526, 2209 — WeakRef husk bug fix); weak-husk guards gc_trace_stw.zig 1577-1584 / 2886-2893; MINOR-AUDIT-WHERE block 2664-2732 + object.zig 6630/2163 helpers; R3 census gc_conservative.zig 389-617 + `tools/perf/r3_summarize.py` (untracked) | do not touch (see §3) |
| 0.3 | gate after each batch | `zig build test`, `zig build test-gc-stress` (ZJS_GC_STRESS=1 / ZJS_GC_VERIFY_MINOR=fatal / ZJS_MINOR_AUDIT=fatal, build/tests.zig:81-83), `mise run gate` (gate_smoke.sh incl. ZJS_GC_ARENA_AUDIT=1 at :110), `mise run stage0` | the S0 spec 裁决门 |

## 1. Ranked table

Columns: rank | id | class | what | files + line ranges | est. lines deleted (non-test; test lines in parentheses) | production impact | knobs & tests removed | risk | prerequisite / ordering.

### Class A — dead / unwired (delete now)

| rank | id | what | files + lines | del | prod impact | knobs & tests | risk | ordering |
|---|---|---|---|---|---|---|---|---|
| 1 | A01 | slab locality audit (MEM-1/M4/CLI-16): comptime-`false` module never parsed | `src/core/slab_locality_audit.zig` 1-729; memory.zig 86-94 (`slab_locality_audit = false`, import 94), 152-155, 340-345, 400, 418, 429, 474, 646, 654, 1922; cli/zjs.zig:407 | 756 | none | none; 0 tests | low | first; removes the last non-memory.zig reader of `build_options.zjs_gc` (slab_locality_audit.zig:551) → enables D02 |
| 2 | A02 | Slot protocol scaffolding never built (SLOT-1/2/3, E-1): gc_slot.zig (prod stub `stats_enabled=false`, only refs are comptime if/else in exec/call.zig whose prod arm is `object.setOptionalValueSlot`), gc_slot_litmus.zig (tests a private mirror struct), gc_write_audit.zig (test-only observer, 47 one-line hooks) | gc_slot.zig 1-232; gc_slot_litmus.zig 1-111; gc_write_audit.zig 1-129; root.zig 21-28, 29-46; exec/call.zig 1745-1750, 1776-1780 (collapse if/else); object.zig 80-89 `auditWrite` + 47 call lines; collection.zig 488-490; generator_state.zig 75-77; core_tests.zig 4-7, 18 | 690 (+~115 tests: core.zig 9068, 9094, 9121, 9141; exec.zig 11082; litmus :56/:85) | none (production arm unchanged); also removes rc-era `dup/free`/`header.retain()` in gc_slot (E) | 0 knobs | low | owner rider C19: `tools/architecture/check_gc_slots.js` (255) + `gc-slots-allowlist.json` (1357) + 27 `// gc-slot:` tags lint the same never-realised protocol (gates.zig 77-80, 129, 149) |
| 3 | A03 | block heap medium/large spaces + generic byte alloc/free (BH-01/02/03, SP-02): F12 "zero production callers" confirmed | gc_block_heap.zig: `alloc` 736-742, `allocSmall` 2192-2205, `free` 795-812, `fromAddr` 337-343, allocMedium/findMediumRun/freeMedium/allocLarge 2453-2520, LargeMap/MediumExtent 236-245, fields 676-677, deinit 705-711, `owns` 1136-1141, `liveBytes` 2155-2158 (→ `liveSmall().bytes`), census `.medium` arm 2099-2103, `SuperblockKind` 196 + `Superblock.kind` + 12 `!= .classed` guards + 2 asserts 1184/1192 + switch 2099 + write 2389 + 2479, `reserveSuperblock(kind)`, Stats live_bytes/live_count/medium_allocs/large_allocs/large_reserves/large_maps/small_allocs(0 writers)/superblock_reserves 118-127; **gc.zig 3576-3580** (`GcObjectIterator.nextCell` kind branch); gc_space.zig `Space` 41-45 + `classifyPayload` 118-122 | 205 | none | tests: 9 `heap.alloc` users (7551, 9748, 9791, 9802, 9829, 9849, 9918, 9948, 10030) → `allocCell(n).?.ptr`/`freeSmallCell` (8 free sites incl. 9967/9971/10047 by computed address); `owns` 9758, 17745, 17856 → `blockOf(p) != null`; asserts 9768-9776 | **med**: completion-plan S2 (:101) plans to reuse medium/large for over-cell strings — recommend delete and re-add a fresh, simpler extent path in S2 (this code never ran in production). `large maps (\d+)` is a required capture (gate_smoke_check.py:50-51, gc_stats_snapshot.py:267, baseline `largeMaps: 0`) → keep printing literal `0` | keep `Superblock.page_bits` 204-212 (classed nonempty index); update pins C18 |
| 4 | A04 | `collectConcurrentMajor` — second STW major, test-only; also mis-counts itself as a minor (GN-9) | gc_trace_stw.zig 912-1000; `countMarked` 2011-2018 (dead after A05); doc mentions gc.zig:4820, stw:1785 | 97 (+~35: test core.zig 16362) | none | 0 knobs | low | before D07 |
| 5 | A05 | `Report`/`last_report` collapse (trace-stw A1/A2/A3/A4/A7/M1/M6/E4): 29 fields, 22 write-only, 0 production reads (runtime.zig 3052-3055/3392-3395 are sticky-gated → `true`) | gc_trace_stw.zig 36 `enabled`, 110-115 `SurvivorClass`, 117-180 `Report` + `format` 154-179, 349 `last_report`, writers 769-772, 782-785, 908-909, 940-941, 957, 984-988, 1269, 1390-1392, run() 1872-1923 (+8), 2327/2350/2389; `exact_mark_count` 1829; `visitRealm2` 2757-2759; `Report.conservative` sink (pass a scratch `Metrics`) | 110 | none | tests: 6 lose one assert each (core.zig 9182, 9278, 9714, 10094, 16036 [skip-gated], 17593) | low | keep `skipped_sweep_incomplete_arenas` as Collector local (prod 756/778) and `ephemeron_values_shaded` as fixed-point local (2272/2292); do with D01 (sweep-model Report fields) and C02 (census fields) |
| 6 | A06 | registry single-winner diagnostic resolution — test oracle only (R2) | gc_address_registry.zig 64-67 `DiagnosticResolution`, 325-337 `resolveInArena`, 339-346, 526-538 `resolveInBlockCell`, 760-800 `resolveAnyInternal`, 802-824 `resolveOneForDiagnostics`, 826-831 `resolveGcForDiagnostics` | 115 | none | tests 6 (19 refs: core.zig 3350, 9329, 9412, 9450, 9472, 17030) → rewrite on `forEachTraceCandidateAt`/`containsHeader` | low | — |
| 7 | A07 | registry string/rope kinds (R1): `insertRange` 0/0 callers | gc_address_registry.zig 30-34 `Kind` arms, 58-62 `Hit`, 49/52-55, 82/411/433 `string_live`, 604-621 `rangeForBytes`/`insertRange`, 653, 684, 753, 793-797, 817-819, 826-831 | 45 | none | 0 | low | **keep `gc.StringHeader`** (live string rc prefix: gc.zig 1247/1251/6254); `Kind` shrinks to `gc_object` (753 reads it) |
| 8 | A08 | gc.zig dead helpers bundle: `HeaderList` + `listDel` (Q1/M3), `listLastAssumeNonEmpty`/`listSentinel` (Q2), `popCycleDeferredFree`+`popForFree` (Q3), `deadCandidateIterator` (A3), `Census`+`census()` (A4), `markPublishedYoung` wrapper (A14), `retainObject` (H9/A2), `ExternalMemoryToken.deinit` (P6), `blockHeapMaxAlign` (G7), `nursery_headroom_bytes` (G8), 2 dead `InvariantError` variants (E1), `hasPendingRequest` (C18), `reportExternalFree` raw hook (A7), `markAssist` tombstone (A13) | gc.zig 1890-1924, 1613-1615, 1623-1630, 2459-2461, 1716-1727, 3796-3802, 3861-3896, 5199-5205, 3420-3423, 655-657, 556-559, 474, `MarkBitLeftSet`/`DuplicateHeapAllocation`, 2843-2845 (runtime.zig:3457 → `hasPendingMajorRequest`), 2735-2739 + runtime.zig 3560-3562, 4685-4690; **A12 is a MOVE**: cut 4361-4392 (barrier-direction rationale) and paste above `shadeForConcurrentMark` 4629 | 150 | none | tests: `DeferredFreeStack.pop` 1708-1714 only if core.zig:36 is edited; `nursery_headroom_bytes` 5 refs (core 12960-13026) rename | low | — |
| 9 | A09 | test-only pub fns in gc.zig (A5/A6/A8/A9/A10): `add`/`addWithSize` + `resetHeaderLifetimeForPublication`, `restoreCycleCandidate`, `verifyNoExternalTokenLeaks`, `nonBlockObjectAuthority{Count,Contains}ForTest`, `forgetGenerationalOwnerForTest` | gc.zig 3025-3042 + 1448-1458, 4347-4359, 6101-6105, 6135-6145, 4929-4937 | 66 | none | tests edited: 7456, 8312 (→ `addInitializedWithSize`), 7471, 7174 (read `external_tokens.len`), 2746/3226 (authority pub → D06), 8150/8193/16010 (`generation.forget`) | low | D06 makes the authority pub |
| 10 | A10 | Realm-teardown route probe (Q6/A11/E3): TLS `if (builtin.is_test) … else void`, body `if (comptime builtin.is_test)` | gc.zig 1769-1818, 5381-5430, call 5369; shape.zig:1072 | 100 (+~40: test core.zig 1235) | none | 0 | low | also E (Shape rc dies in S1) |
| 11 | A11 | `Policy` dead fields (P1/P2/M1): `Mode`, `Policy.mode` (write-only), `forMode` (tests only), 3 slice budgets read only by dead `sliceBudgetNs` | gc.zig 566-571, 574, 578-580, 609-637, 2869-2876 | 50 | none | test "gc policy presets configure real pressure and slice controls" (core.zig 6881) dropped | low-med: `gc.Policy` is the JSRuntime init option `gc_policy` (runtime.zig:321) and `GCPolicy` re-export (core/root.zig:147, internal_root.zig:39; **not** in public src/root.zig) → embedder-visible struct change, owner ack | pair with B04 |
| 12 | A12 | marking-group dead code: `Queue.pop/popSingle/popImpl` (MQ-4), `StatsStorage`/`notePushLocked` (MQ-5, 0 readers even in tests), safepoint/critical-scope protocol (CC-1), `remembered_owners` test mirror (GN-3 — latent false positive under ZJS_GC_ARENA_AUDIT in ReleaseFast), `AddressLookup` (CS-1), `missing_abis` (CS-2), dead `Metrics` fields (CS-3), `parallel_threshold` | gc_mark_queue.zig 501-542, 314-318, 329, 358-362, 383-385, 495, 511, 531, 551-556 + 6 sites; gc_concurrent.zig 12-17, 36-39, 133-136, 164-186, 189-201; gc_generation.zig 21-23, 35, 226, 234, 306 + gc.zig 5899-5901, 5918 (→ `remembered.count()`); gc_conservative.zig 63-113, 11-12, 50-52, 55, 58-60, 365-368, 137/316-318; gc_parallel_mark.zig:54 | 195 | none | tests: 16447, 16491, 16525, 16559 (drain via stack); 16313-16336 deleted; 8414-8416/16174-16176 → `remembered.count()`; CS-3 asserts core.zig 9403-9404 need a new oracle if C10 also lands | low | — |
| 13 | A13 | memory.zig dead API (M8a/M11/M12/MISS-1/MISS-3): `allocAlignedBytes`, `createConstFamNoTrigger`, `createWithFamNoTrigger`, `SmallObjectSlab.alloc/.free`, `debugArenaCount`, `accountedSizeForRequest`, `oomCoverageReset`, `isGcObject` → `@hasDecl(T,"gc_kind_tag")`, `JSRuntime.createWithTrace`, registry `enabled` | memory.zig 1106-1108, 1192-1198, 1701-1703, 317-320, 373-376, 666-671, 762-765, 166-171, 1226-1236; runtime.zig 1428-1432; gc_address_registry.zig:20 | 49 | none | tests: in-file 2133-2165 helpers moved; core.zig:14856, tests/bytecode.zig 3159-3162 inline `accountedSizeForRequest` | low | **`block_header_bytes` 263-264 is NOT dead** (prod use memory.zig:756) |
| 14 | A14 | block heap small dead items (BH-05/08/10/11/12 + missed): `censusCellFacts`, `Stats.committedLiveMilli`, `flag_remembered/overflow/bailout`, `fromAddrChecked`/`cellIndexTrusted`/`bitWord`, `enabled`, `serves_gc_nodes`, `hot_reuse_k32/k128`, `superblock_reserves`, glued docs | gc_block_heap.zig 1118-1134, 154-157, 306-308, 333-335, 413-415, 2641-2643, 23, 97-112, 71-73, 75, 126 + 2365, 1-9, 204-209, 279-286, 316-317, 2649-2663 | 70 | none | 0 | low | **keep line 74 `hot_reuse_k64`** (read via 76 → openBlock 2251) |
| 15 | A15 | object_gc.zig unused imports (O1) + dual naming of `drainCycleDeferredFrees` (O2) | object_gc.zig 7, 8, 14, 16, 19, 21, 23, 27; 29-31 or object.zig 7115-7116 (pick one name) | 13 | none | 0 | low | — |
| 16 | A16 | `-Dzjs_experimental_gc` compat alias (0 consumers; comment "gate scripts still pass it" is false) + two `gc-shadow-check` arg tests for a flag that no longer exists | build.zig 75-79; cli/zjs.zig 1592-1596; cli/run_test262.zig 1040-1044 | 15 | none | 2 tests | low | — |

**A subtotal ≈ 2,720 lines (+~190 test lines).**

### Class B — default-OFF arms (delete arm + knobs)

| rank | id | what | files + lines | del | prod impact | knobs & tests | risk | ordering |
|---|---|---|---|---|---|---|---|---|
| 17 | B01 | **carrier authority layer wholesale** (CAR-1/C1/M1/BH-04/G3/D6/C15/C5-arms): `production_authority = .{}` all-false → every `*_enabled` reduces to `builtin.is_test or -Dzjs_ownership_audit`; `productionExactCapability` is an unconditional `@compileError` never instantiated; a 2-point lattice whose production point is empty; shadow oracle verified only against itself | gc_carrier.zig 1-468; memory.zig 17, 25-30, 717-723, 1260-1414, ~85 call-site gate lines (1508-1908), pin 2070-2098; gc_block_heap.zig 198-201, 210-234, 693-696, 712-717, 829-1049 (interleaved with prod `popCell` at 881 — net ~300), 1097-1105, 1935-1942 + `require_young_membership`, 2210-2222, 2293-2311, 2329-2352, 2366-2381, 2420-2426, 2523-2532, test 2536-2560; gc.zig 90-106 (re-exports, `ResolvedExact`/`ResolvedCurrentMember` single-arm unions), 264-267/280-283/337-339 `carrier_inject`, 525-554 oracle types, 2406-2407, 2684-2690, 3155-3161, 3215-3221, 3304-3312, 5106-5109, 5934-5950, 5978-5999, 6028-6096, 6169-6243 (`resolveCurrentMember`/`allocationHandle`/`resolveExact`); object.zig 1706-1720, 1730, 1732, 2062, 2064; object_gc.zig:110 | ≈1,300 (+~150 tests: core.zig 3350 partial, 9412, 17054, 17120, 17137; gc_carrier 427-468; block heap 2536-2560) | none (comptime-off in every shipped build) | `ZJS_GC_CARRIER_INJECT` (test-only; also memory.zig 723/1366); **keep `-Dzjs_ownership_audit`** (atom.zig 18/21/992, class.zig 418; nightly.yml:40) | low-med: loses an ABA/stale-handle oracle nothing relies on; nightly `-Dzjs_ownership_audit=true` still builds. Alternative C2: keep only the `HeapAccountingOracle` byte shadow ledger + gc.zig 6028-6032 cross-check (−~400 instead) | do before/with B02 (the oracle is stored in the `sticky_major` field, gc.zig 2403-2407); update pins C18; unblocks D08 `Cell.index`, C17 |
| 18 | B02 | **sticky-major experiment** (K1/B1/B-1/BO-3/CLI-5a): build option default false, never compiled in any shipped/gated artifact; 4 tests all `SkipZigTest` | gc.zig 40-41, 231-257 knobs (keep `verify_major_all` 249 + parser 316-320 under `roots_diag_enabled`), 320-333 env, 486-522 `StickyMajorStats`, 532-553 state (oracle carve-out → rename), 2406-2407, 4392-4521 (`stickyMajorFullPressureExceeded`, `noteFull/IncrementalCollectionSettled`, `stickyMajorStats`, `stickyMajorFullBaseline`; **keep** `IncrementalMajorScope` 481-484, `prepareIncrementalMajorScope`/`incrementalMajorScope`, `noteStickyMajorOracle` re-plumbed to `.full`-only while roots_diag lives); gc_trace_stw.zig 1062, 1073-1097 `.sticky` arm → `collector.clearMarks()` (**keep** 391-397, 482-516 `verifyStickyCondemnation` scope→full, 1230-1248 oracle block — shared with roots_diag); runtime.zig 3048-3056, 3179-3180, 3389-3396 (→ `true`); cli/zjs.zig 938-966; build.zig 81-91; build/config.zig 121, 142 | ≈280 (+~220 tests: core.zig 15938, 16036, 16067, 16096) | removes default-off arm | `-Dzjs_experimental_gc_sticky_major`, `ZJS_GC_STICKY_MAJOR`, `ZJS_GC_VERIFY_STICKY_MAJOR`, `ZJS_GC_STICKY_INJECT_SKIP` (**`ZJS_GC_VERIFY_MAJOR_ALL` stays**) | low; CLI rows parsed by no tool | docs: docs/every2-arm-2026-08-29.md, docs/splay-account-2026-08-28.md:196-207 (the four docs the cross-cutting survey listed are about sticky *mark bits* — no edit) |
| 19 | B03 | **parallel marking** (PM-1..5/B-2/V7/CLI-8): default `parallel_mark_min_live_bytes = maxInt` → 0 workers, `ensureSpawned` never reached; S4b verdict "default off" | gc_parallel_mark.zig 1-545 (relocate `footprint: stw.MarkFootprint` 154-157 to Registry/stw global; readers stw:703, cli:1137, tests 17582-17590); gc.zig 164-182, 293-299; gc_trace_stw.zig 1052-1057, 1147-1180, 1345-1348 (`last_settled_live_bytes/cycle_live_bytes/slice_in_cycle/cycle_hot` have no reader outside the gate); runtime.zig 1217, 1758; cli/zjs.zig 429, 1127-1134 | ≈620 (+~108 tests: core.zig 16596, 16703, 16712, 16720, 16725 + helpers 16621-16630) | removes default-off arm; also removes the `Tracer` copy of shadeExact (with rc-era `cycle_visited` check 411 and `frontierInjection(1)` 418) and the Windows `RtlWaitOnAddress` arm | `ZJS_GC_PARALLEL_MIN_LIVE_MB`; row `gc: parallel mark claims` unparsed | low | unlocks D03 (mark-queue collapse) and CC-4; stale docs stw 60-61, 692-694, gc.zig 2393-2401, 4159-4164, docs/gc-v2-s4b-parallel-marking-gate.md |
| 20 | B04 | process-memory (RSS/cgroup) + external soft/hard pressure policy (P3/P4/B2/M3/M7): no in-tree production setter; `gcStats()` does `/proc/self/statm` + cgroup reads on **every** public call | gc.zig 585-590, 603-608, 883 `.rss_pressure`, 898-901, 918-922, 2798-2812, 2781-2783, 2785-2787, 2791-2796 (→ `.soon`), 2172-2173; runtime.zig 3597-3598, 3606-3631, 3649-3670 | ≈130 | inert arm removed; removes two file reads per `gcStats()` call | 0 knobs; tests core.zig 6952, 6984, 7093 (7136 `rss_bytes != 0`), runtime.zig in-file 4908-4936 | med: `Policy` embedder-facing (see A11); not printed by cli, not parsed by tools | keep `externalMemoryRequestReason` `.allocation_debt` branch (2784, prod) and `major_debt_threshold`/`external_weight`/`large_object_threshold`/`native_cleanup_slice_jobs` |
| 21 | B05 | OOM-injection coverage (`-Dzjs_oom_coverage`, M3) — not GC-specific | memory.zig 32-47, 127-150, 157-171, record() 948, 1117, 1483, 1673, 1725; build.zig:37; build/config.zig 110, 137 | 60 | none (comptime-off) | tests/oom.zig 1424-1433 early-returns | low; owner call (out of GC scope) | — |

**B subtotal ≈ 2,390 lines (+~480 test lines).**

### Class D — duplicated / layered (collapse, behaviour-preserving)

| rank | id | what | files + lines | del | prod impact | knobs & tests | risk | ordering |
|---|---|---|---|---|---|---|---|---|
| 22 | D01 | **sweep model** (SM-01/D5/D-2/BH-09/G2) — functionally dead: production drives an empty window map (`noteAllocated` only under `sweep_model_stats_enabled = … and builtin.is_test`), `endMark(0)` "zero, deliberately", no CLI row, `Report.format` never called; reclassified D only because `Block.sweep_state` is production storage | gc_sweep_model.zig `Model` 28-200 (relocate `SweepState` 13-26 into gc_block_heap.zig **or** drop `sweep_state` per BH-09: field 259, writes 2233/2249/2274/2280-2281/2437, verify reads 1545/1591-1602/1806/1840/1861/1881); gc.zig 58-66, 84-87, 2331, 2678-2680, 5342-5347; gc_trace_stw.zig Report fields 132-152 + format 160-177, 647-652, 728-737 drain ritual, 1257, 1381, helpers 1932-1984, run() sites 1879/1904-1905/1920-1924; root.zig 90-96 | ≈350 | none (only readers are tests) | tests core.zig 9665, 9714, 10094; block heap 7568-7571, 9762 | low | with A05; removes block heap's only `gc_sweep_model` import |
| 23 | D02 | **always-true comptime gates + single-valued `-Dzjs_gc`** (G1/BO-1/BO-9/D2/M2/SP-04/CLI-2/MISS-2): `trace_stw_enabled = true` (0 external readers — gc.zig:29-33 doc stale) and 6 aliases; `build.zig:63` accepts one value | gc.zig 27-34, 50, 54, 58, 69, 89, 376, selectors 74-87, 476-479, 561-564, 469-470, 2676-2677; root.zig 74-103 stubs; memory.zig 23-24, 57-58, 84/348 (`slab_alloc_prefetch`), conditional types 251, 276-277, 282-283, 700-701, 715-716, early-outs 290, 553, 578, 1929, 1995, 2002, 2007, wrappers 421, 434, 453, 472, 477, 622, asserts 1267…1902, `block_heap_enabled and is_gc` 1497/1611/1732/1851, pin 2080-2081; gc_trace_stw.zig 30 gates with dead else-arms (798, 924, 1117-1118, 2239, block_heap ×18, sweep_model ×7); cli/zjs.zig 420, 427, 433; gc_conservative.zig:369; ~95 `comptime if` wrappers elsewhere (block_heap 60, generation 16, address_registry 4, space_model 3); test `SkipZigTest` guards (e.g. core.zig 16314, 16363); build.zig 63-79 + build/config.zig 104-110, 118, 128, 141 | ≈275 | none | `-Dzjs_gc` option (keep a 3-line rc/shadow migration error if desired) | low | S0 spec:14 / gc.zig:29-33 deferred cross-file constants to S5 — content is free now; **owner call on timing only**; do after A01 |
| 24 | D03 | **gc_mark_queue single-thread collapse** (MQ-1/2/3/8/9, CC-4, D3, stw M3): doubly-linked segment chain + donate/steal, two pthread mutexes (incl. `Queue.len()` locking on every `incrementalMarkStep` at stw:1169), 3 push variants, two-tier push, wrapper-over-wrapper allocators, atomic `major_marking_active` with `comptime order` for a one-thread flag | gc_mark_queue.zig 25, 198/202, 281-304, 472-499; 78, 323 + ~38 lock lines (105-141, 432-470 dance); 402-404, 432-470; 331-349, 200-206, 233; header 1-14; `Segment.item_capacity` 29-30 + pin 36 + `MarkStack.reset` 213-218 rewrite; gc.zig 980, 5064-5081, 4824-4827; gc_trace_stw.zig 2089-2093, 1169, 1182, 2250/1199; gc_concurrent.zig 130-132, 152-163 | ≈420 (557 → ~150) | behaviour-preserving; removes mutex lock/unlock from the per-slice path and every frontier push | tests 16439/16490/17248 → `pushSingle`; 8 prod + 15 test `setMajorMarkingActive` sites lose the `order` arg; keep 17254 OOM fail-closed test (`failBackingAllocationsForTest`, drop the Queue wrapper 339-342) | low-med (mark frontier; mechanical) | after B03; keep both `MarkStack.pop` variants (non-prefetch has prod caller stw:2243) |
| 25 | D04 | RefKind descriptor catalogs (R1/R2/R3/R4/H6): every column is a function of `kind` already expressed elsewhere and comptime-asserted equal | gc.zig 687-795, 3292-3294 `isCycleCandidate`, 811-841 (`refCountRemoved` ≡ `frontierEpochSafe`), 1016-1032 → keep `traceRememberedCacheEligible` + `refCountRemoved` + two `kind ==` tests; rewrite `verifyMetadataSemantics` 2024-2105 (~15 lines) and **src/gc_representation.zig:17-26** (keep that module — see §3) | ≈145 | none | regenerate `src/gc-representation-trace-snapshot.txt` (core.zig:10-20 compares against it) | low | — |
| 26 | D05 | Stats duplication + dead stats fields (E4/D7/C4/E5/A16/M2/M4/C17/D12): `GeStats` vs `Stats` copied 1:1; fields with zero prod/CLI/tool readers; `major_phase`/`major_reason` state machine whose only consumers are 2 test asserts (`abortMajorCycle` ≡ `finishMajorCycle`) | gc.zig 2128-2164 vs 2166-2217 + 2978-3025 → one struct; delete `total_allocated_bytes`, `old_allocated_bytes`, `old_alloc_count`, `large_allocated_bytes`, `large_alloc_count` (2983-2996), `major_gc_time_ns`/`cycle_gc_time_ns` (2132/2194, 3009, 5507), `gc_request_count` (2158/2211, 2815, 3017), `pending_major/_request_reason/_request_urgency`, `pinned_cell_count` (2202, 3016), `last_failure`/`FailureKind`/`last_collection_time_ns` (1928-1932, 2134-2135, 2195/2199, writers 5497-5567), `MajorPhase` 864-868 + fields 2283-2284 + 2889-2915 (runtime.zig 3022-3047, 3229 callers) | ≈115 | none (`heap bytes` row uses `heap_live_bytes`/`peak_allocated_bytes` only) | ~15 test asserts (core.zig 7338, 17313, 9065, 17299, 17314, dup-field refs 5/4/2/6/3; runtime.zig in-file 4922-4924) | low | `zero_ref_drains` handled in C15 |
| 27 | D06 | `NonBlockObjectAuthority` forwarders + registry layering inversion (D9/M8/R5): 9 one-line forwarders over a private struct; authority pointer stored as `?*anyopaque` in the lower `Table` | gc.zig 4034-4061, 6126-6150 → pub Registry field; gc_address_registry.zig 153-159 + gc.zig 2669, 5112, 6148 `@ptrCast(@alignCast)` → typed field | 50 | none (`doomedNonBlockObjectCount` value feeding gate_smoke doomed rows unchanged) | 0 | low | with A09 |
| 28 | D07 | **unify the two major implementations** (stw D1/D6/D8): STW `collectCycles`→`Collector.run`→`sweepUnmarked`→`destroyCondemned` vs incremental `begin/step/finish`→`destroyDoomedSlice`; same 5-pass kind order twice; `collectCycles := abortIncrementalCycle(); beginIncrementalCycle(); while (!incrementalMarkStep(maxInt)) {}; finishIncrementalCycle(); finishPendingDestruction()` = runtime.zig 3246-3315 `forced` path; double `block_heap.beginMajor()` at 739 + 1991 (D6) | gc_trace_stw.zig 727-786, 1870-1926, 2542-2631, 1990-1993, 2011-2018, 1928-1930 (+ `destroyCondemned` 2844-2999 and `KindSet` 2821-2842 only if the minor also condemns to the morgue: −170 more); runtime.zig bookkeeping merge 3049-3060 vs 3384-3399; `stats.collections += 1` at both 738 and 1209 | ≈220 (+170 optional) | behaviour-preserving refactor (changes code path, not observable semantics) | 2 direct `collectCycles` tests | **med**: largest single refactor; must keep `recordSuccess` feeding the pause ring (`gc: major pause` row :257) and the precision promise (runtime.zig 2988-3004 abort-then-begin) | after A04, A05, D01 (removes the deltas: censuses, sweep hooks) and B02 (sticky arm in `begin`); run the full safety-net trio + test262-stress |
| 29 | D08 | gc_space.zig comptime generator chain → literal table (SP-01/SP-05/BH-13/14/15 + `percentileOf` dead param): production reads only `classes`, `class_count`, `classIndexForPayload`, `min_cells_per_block`, `large_min_bytes`, `max_small_payload`; three definitions of the 8-byte prefix (gc_space.zig:20, gc_representation_constants:6, gc.zig:1072) | gc_space.zig 12-13, 20, 25-39, 47-99, 101-116, 127-133, 146-152, 238-256, 219-236 `_ = large`; gc_block_heap.zig triple-redundant guards 169-173, 179-185, 736-742, 755-758; `Cell.index` 164-167/755-761 → `?[*]u8` (only consumer `setReuseSequenceForTest` 982-986 dies with B01); `containsInitializedBlock` 2172-2174 | ≈115 | none | tests core.zig 9544-9662 generator asserts (keep the histogram half of 9588); 7527/7556/17148 `.ptr` only | low | after B01 |
| 30 | D09 | gc.zig wrapper/alias collapses (D1/D2/D4/D10/D11/S2/G9/G6/D8/D5/stw M2/M3/GN-6/GN-7/CC-3/percentileIndex): `defaultHeapBytes`+`unlinkObject` (context.zig:540 → `unlinkObjectWithBytes(&h, @sizeOf(JSContext))`); `registerLiveAddress`/`markPublishedYoung` wrapper pairs; six `GcObjectIterator` ctors → one + filter enum; 3 identical `observeSlabArenas` closures; `clearYoungBlockMarksStw` forward; `phaseIsTwoPassTeardown` inline; type aliases `GcKind`/`StringHeader`/`Header`/`GCObjectHeader`/`ObjectHeader`/`MarkStack`; `StickyMajorStateBase/WithAccountingOracle` → one struct; construction-root predicates `isConstructionRoot` 3722-3742 → `verifyConstructionRoots` + `blockCell*Allowance` 3751-3796 verifier-only (no measurable prod cost — `pinEntryIsConstructionRoot` short-circuits on `count == construction_pin_count`); `FrontierSafeHeader` newtype (all checks `comptime runtime_safety`) → `*Header` + one debug assert; `shadeOptionalObject`; redundant trailing `checkFrontierFailure`; `isYoung(_ = self)` + one-line wrappers; duplicated `percentileIndex`; size-pin churn CC-3/GN-7 | gc.zig 3229-3253 + 3415-3418, 5147-5155, 3798-3855, 5125-5141, 3857-3859, 859-861, 797/980/1247/1270-1272, 532-553, 3688-3796, 1340 + 4102-4183; gc_trace_stw.zig 2102-2105, 2250, 1199; gc_generation.zig 204-207, 410-416, 101-107, 192-195 vs gc.zig 2944-2947; gc_concurrent.zig 102-108 | ≈250 | none (D5 touches the hot mark path but only erases comptime-dead checks; O2-B wording in docs/gc-invariants.md is not read by any gate) | 1 test (16494 `frontierSafeHeaderForRequeue`) | low | D5 needs gc_mark_queue signature change → with D03 |
| 31 | D10 | memory.zig allocator layering (M8b/M9/M10/R6 low-risk; M14/R7 medium): `allocation_gc_trigger_enabled = is_test or force_gc` threads `comptime trigger_gc` through 8 fns + `createWithFamComptime` third copy; `small_slab_enabled` runtime flag at 23 sites; `gc_object_cell_heap: ?*Heap` `orelse` on the hottest alloc path; incomplete-flag bit packing; occupant-insert OOM replay + arena observer/resync | memory.zig 121, 947-1007, 1116-1144, 1479-1568, 1669-1699, 1720-1802, 1960-1973 (~76; gate runtime.zig 2508-2513/2529-2534 save/restore behind the comptime); 23 `small_slab_enabled` sites (~25); 715-716 + 1317, 1336, 1344, 1354, 1500, 1616, 1734, 1853 (8); gc_address_registry.zig 111-114, 197-219, 696-701 (20); **medium**: 884-887, 857-882 + gc.zig 5189-5193, 5590-5604 (R7 ~50) and memory.zig 201-223, 278-283, 422, 435, 478 + gc.zig 5115-5145, 2323/5118/5587, 5586-5589 + registry 125-146, 197-284 (M14 ~120) | ≈130 low-risk (+~170 medium) | behaviour-preserving for M8b/M9/M10/R6 (codegen changes on the alloc path → Stage-0 quick screen); M14/R7 change OOM recovery (fail-fast vs leak-but-safe) → owner call | keep `trigger_gc_fn` test hook (27 test refs) and `-Dzjs_force_gc` tier; test 16036 sets `arena_slab = null` (skip-gated) | low / **med** | — |
| 32 | D11 | root-scan parameter and `extra_roots` plumbing (stw D3/D4): production ignores `scan` (`Collector.init` 1855-1858 uses `!host_quiescent` unless `is_test`); `extra_roots` non-null only from tests via public `runObjectCycleRemovalWithValueRoots` | gc_trace_stw.zig 5 entry-point signatures + `computeFullReachable`, 1855-1858, 2183-2187; runtime.zig 383-387 `rootScan()`, 1270/1492/3452 `test_root_scan_override` → test-only global | ≈25 | none | 0 | low; `extra_roots` is public API — UNDETERMINED whether an external embedder passes non-null | — |
| 33 | D12 | two ways to force collection: `-Dzjs_force_gc` (`force_gc_on_allocation_enabled`) vs `ZJS_GC_STRESS` | memory.zig 59, 121; runtime.zig 3720-3721, 4927; object.zig:1833; build.zig:38; build/config.zig 63/138 (changes the `zjs-config-v3` signature string) | ≈15 | none | 0 automated users | low; owner call (signature change) | — |
| — | D13 | **deferred, not in this pass**: fast/cold publication twin (gc.zig 3067-3096 + arm threading, ~45 — perf-motivated "five callee-saved pairs on 255M EarleyBoyer publications", needs Stage-0 A/B) and request coalescing/`SchedulerPoint` (2814-2841, 2862-2887, 870-906, ~60 — urgent-vs-soon semantics, test 7333) | — | 0 | changes codegen / semantics | — | owner decision | — |

**D subtotal ≈ 2,110 lines (+~340 conditional: D07 optional minor path, D10 medium).**

### Class C — diagnostics (safety net excluded; see §3)

| rank | id | what | files + lines | del | prod impact | knobs & tests | tools read it? | risk | ordering |
|---|---|---|---|---|---|---|---|---|---|
| 34 | C01 | unparsed CLI rows + CLI-only stats (BH-06/CLI-3b/4/5c/6/7-recon/12-none, MQ-7, failed_reserves, hot-block test counter): block-heap topology/cell-capacity/per-class census; `gc: Object publications total`; `gc: slots2 payload attaches`; `gc: mark frontier segment bytes` + `PoolStats` shrink; uncommitted reconciliation row; `Stats.failed_reserves`; `publish_completed_hot_blocks_calls_for_test` | gc_block_heap.zig 2062-2150 + cli 1021-1063 (**keep 1064-1071 `major threshold resets` row and 1072-1088**), test 9926/9935 → `liveSmall()`; cli 825-829 + gc_space.zig 165-167, 185-189, 191-193 + gc.zig 5331-5335/5357-5361; cli 425-428; cli 924-935 + gc_mark_queue.zig 47-72 → keep `active_segments`+`allocation_failures` (+`peak_active_segments`/`cached_segments` or rewrite 16442, 16450, 17363); cli 1110-1120; gc_block_heap.zig 128 + 6 increments (tests 9798/9819); 25-28, 1342 (test 15577-15592) | ≈210 | none | `--gc-stats` only | **no** (verified: none of these labels appear in tools/) | low | — |
| 35 | C02 | injection mutant harness (V13/C-3/A8/B3/Q4/H11): operator-run, never automated (0 users of the env in build/, tools/, mise.toml, .github, src/tests); mutation 2 arm has no selector in any build; documented only in docs/reports/gc-v2-20260901/s3pre-frontier-safety.md:83-96 | gc.zig 263-290, 334-343; arms 1288, 1312, 1324, 1691-1703, 3113-3117 (unreachable), 5109, 6214 (B01); gc_trace_stw.zig 1143-1146, 2067-2084 (un-nests shadeExact); gc_parallel_mark.zig 418 (B03); object.zig 6619, 6647; memory.zig 723, 1366 | ≈65 | none (comptime-erased) | `ZJS_GC_FRONTIER_INJECT`/`CARRIER_INJECT`/`M_CUT_INJECT`; core.zig:483 reads `m_cut_inject == 3` (1 edit) | no | low; owner call — all-or-nothing across 3 files + doc | — |
| 36 | C03 | threshold/headroom/growth env overrides (V4/V5/V6/C-4) — 08-31 campaign sweep knobs | gc.zig 138-162, 348-360; runtime.zig 3857-3864, 3875, 3893 | 45 | removes 3 runtime branches in threshold computation | `ZJS_GC_MIN_THRESHOLD`, `ZJS_GC_HEADROOM`, `ZJS_GC_GROWTH_PERCENT`; test core.zig 12975-13024 | no | low | — |
| 37 | C04 | destroy probe (V14/C4/C-2) — raytrace slice decomposition one-off | gc_trace_stw.zig 1438-1447, 1550-1553, 1589-1594, 1632-1637, 1668, 1692-1695, 1729-1732; gc.zig 351-354; cli 986-995 | 45 | removes `if (destroy_probe)` branches in `destroyDoomedSlice` | `ZJS_GC_DESTROY_PROBE`; 0 tests | no | low | — |
| 38 | C05 | `ZJS_GC_STRESS=off` / `ZJS_GC_NO_MINOR` (V2/V3) | gc.zig 130, 135, 345-348, 2879, 4698 | 20 | none | 0 tests, no gate | no | low (owner list names `ZJS_GC_STRESS` only) | — |
| 39 | C06 | `zero_ref_drains` never incremented anywhere (A15/E8) | gc.zig 2129, 2196, 3011; cli 1217 | 4 | none | tests 1267/1300, 8613/8637 assert equal-to-before | **yes** — token required by gc_stats_snapshot.py:242 → print literal `0` (no tool edit) or edit regex + fixture :23 | low | — |
| 40 | C07 | registry `Table.Stats` + `mutation_stats_enabled` (R3): no production reader, "not exposed through --gc-stats" | gc_address_registry.zig 21-25, 69-93, 118, 24 increment sites, 428-436/459-465 mirror checks; gc_conservative.zig 338, 356 | 72 | none | tests 9329 (`failed_inserts`, `arenas_live`), 9472 (`live`, `pages`) lose asserts; pick a different oracle for A12/CS-3 (9403-9404) | no | low | gc_generation.zig:23 has its own `mutation_stats_enabled` — untouched |
| 41 | C08 | verifier trims, semantics unchanged (stw C5/D9, BH-16, C8): table-driven `verifyCollectorInvariants` (PLAUSIBLE −40, receivers differ); block-heap verify inline print blocks; `verifyPublishedCells` wrapper; de-pub `auditLiveObjectsResolve`/`auditDeferredPayloadRootsBeforeBlockPublication`; move `verifyObjectPropertyStorageLayouts` to object_gc.zig | gc_trace_stw.zig 601-687, 1795-1820, 1514-1526; gc_block_heap.zig 1669-1672, 1739-1742, 1749-1752, 1760-1763, 1988-2001, 1909-1915; gc.zig 3907-3986 (move) | ≈80 | none (all under `invariantChecksEnabled`) | tests 7629-7650 → `…Allowing(…, null)` | no | low | keep every check — ZJS_GC_ARENA_AUDIT safety net |
| 42 | C09 | `AllocInfo.large` audit-only bit + `Stats.large_object_bytes` (H4): nothing in allocator/collector branches on it | gc.zig 968, 1094, writes 3036/3127/3171/3213, reads 2040/2051/2082/2097/3303/3307/5969, 2171/2952-2996/3349; gc_representation.zig `alloc_info_large_mask` | 15 | none | 8 test refs | no | low | S4 header rewrite touches flags anyway — may defer |
| 43 | C10 | size-pin churn (BH-21/CC-3/GN-7/memory pin): every cut above otherwise pays an edit here; keep only layout-bearing Block pins | gc_block_heap.zig 187-194, 223-234, 2523-2532 (keep 298-303, 623-625); gc_concurrent.zig 102-108; gc_generation.zig 101-107; memory.zig 2070-2098 (with B01) | 55 | none | 0 | no | low | first or last |
| 44 | C11 | external-memory token id table + external counters (M1/M2/C16/E7): O(n) `externalTokenIndex` on every tracked release; only adds double/invalid-release + leak detection over `stats.external_bytes ± bytes` | gc.zig 642, 903-906, 2277-2279, 2697-2705, 2749-2779, 3354-3367, 3375-3381, 3425-3440, 2649-2656, 6090-6098, 4 `InvariantError` variants, Stats 2152-2156, 2185-2187; cli 1225-1238 | ≈130 | drops a linear scan per ArrayBuffer release | tests core.zig 7174, 7093, 4663; runtime.zig 4883-4924 | no (`gc: external bytes` unparsed) | **med**: loses double-release detection (qjs has only a malloc-size ledger) — owner call | — |
| 45 | C12 | memory.zig diagnostics (M5/M6): `-T/--trace` alloc tracing (NO-OP in ReleaseFast: `diagnostic_accounting_enabled = is_test or Debug`) and `alloc_calls/free_calls/create_calls/destroy_calls/peak_allocation_count/profile_alloc_count` | memory.zig 707-708, 776-778, 2052-2067, 865-870, 932, 1026, 1095-1099, 1158, 1604, 1843, 1907; runtime.zig 318, 1404-1432; cli 54, 100-103, 251, 1605-1607; memory.zig 693-709, 916-945, 1159-1161, 1914-1916, 1989, 22 call lines; runtime.zig 334-339, 3522-3527; cli 645-651, 703, 1335 | ≈95 | none | `-T`; ~70 test lines (32 `alloc_calls` + 34 `create_calls` leak-balance oracles; bytecode.zig 6122-6124) | **yes, non-gate**: tools/perf/diff_runtime_profile.js:30-35, tools/perf/same_runtime/zjs_same_runtime.zig:529-548 (already treat counters as Debug-only) | med (test churn) — owner call; **keep `allocation_count` + `peak_allocated_bytes`** (`account peak` row :247) | — |
| 46 | C13 | cycle envelope S/T/B/P (C1/CC-2/M7): §1.3 design measurement; memory.zig side is **not** gated on `detailed_reports` — a runtime null-check on 5 allocation-account paths + 8 B/MemoryAccount | gc.zig 4553-4627, 2507-2508, 4528, 5535; gc_concurrent.zig 56-64, 110-127, 140-149; memory.zig 20-23, 696-701, 1975-1982, 1992-2009, 2080, 769, 892, 1094, 1130, 1151; runtime.zig 3211, 3466, 3898; cli 966-981 | ≈165 | removes a hot-path branch | test core.zig 17630 | **yes**: gc_stats_snapshot.py:371-374 required row + cross-checks 458-487; fixture | low-med: coordinated tool edit + baseline re-freeze (stage0 STOP metrics do not use it) | owner call |
| 47 | C14 | minor pause distribution + minor phase totals (GN-1/GN-2) | gc_generation.zig 60-70, 90-98, 109-116, 144-150, 163, 171-174, 177-195; gc_trace_stw.zig 825-905 `phase_stats` arms; cli 865-890 | ≈110 | none (retained only under `--gc-stats`) | tests 15604, 16986 | **yes**: rows :342, :353-354 (+ check :446); stage0 METRICS read only `minor STW total` (always-on `pause_ns_total`) | low-med: tool edit + baseline | owner call |
| 48 | C15 | space histogram (SP-03/D-1/C13): consulted by no allocation or scheduling decision; 2,096 B per Registry; one `detailed_reports` branch per publication | gc_space.zig 155-236; gc.zig 5325-5362, field 2328; cli 805-824 | ≈140 | none | test 9588 | **yes**: `gc: allocation histogram` row :262 | low-med: tool edit + baseline | owner call; partial (C01) free now |
| 49 | C16 | **mark-footprint census** (C3/C-1/CLI-9/C2): `--gc-mark-footprint` opt-in walk (documented cost splay −9.8% when on); M-cut/obj64 study closed | gc_trace_stw.zig 181-347 (incl. in-file test 322-347), 544-562, 563-599 census timing (conditional on this: `last_census_ns`, `last_finish_remark_raw_ns`, `censusTimed/Start/End`, runtime.zig 3041/3323 deduction), 688-725 + calls 969/1225/1893, 227/261 `refcount_removed_headers`; object.zig 7296-7479; cli 129-138, 1136-1202; gc_parallel_mark.zig:157 | ≈490 (+~90 census timing) | none | tests 17562, 17593, stw 322 | **yes, hardest coupling**: gc_stats_snapshot.py:1055-1056 runs every fixed-work bench with `--gc-mark-footprint` and hard-requires 15 rows (:388-437) + consistency checks (:515-560); verify/test_gc_stats_snapshot.py:51, 210-218; stage0 STOP metrics use none of the keys | **med**: tool + policy change + frozen-baseline re-freeze (`shared-h_pre0/main-d944f26d`) | owner call; if kept, B03 must relocate `footprint` |
| 50 | C17 | pause ring shrink (E3/C2/M5): `pause_sample_capacity = 1024` = 8 KiB of the ≈10.5 KiB Registry | gc.zig:2110 → 64-128 | 0 | none (label `gc: major pause … retained N of M` and public `GCPauseDistribution` unchanged) | 0 | yes (unchanged) | low | — |
| 51 | C18 | stale comments describing removed collectors / deleted modules (A9-stw, BH-12, GN-8, CS-7, MISS-4, M4-gc, G10 history prose) | gc_trace_stw.zig 1-11, 60-61, 692-694; gc_block_heap.zig 1-9; gc_generation.zig 1-15; gc_conservative.zig 1-3, 11-12; memory.zig 20-22, 88-93, 712, 1464, 2016-2020; gc_address_registry.zig 4-9; root.zig 21-22 … 104-105; gc.zig 29-33, 388-470, 2393-2401, 4820; object.zig 82-84; object_payloads.zig:742; docs/gc-inventory.md 168-179, 320, 645-657 | ≈120 comment lines | none | — | no | none | with the code they describe |
| — | C19 | owner decision, not code: `check_gc_slots.js` + allowlist lint (see A02) | tools/architecture 255 + 1357; build/gates.zig 77-80, 129, 149 | 0 counted | gate cost only | — | — | — | with A02 |

**C subtotal ≈ 1,860 lines: ≈ 730 free now (C01-C10, C18), ≈ 225 owner call (C11, C12), ≈ 905 need tool/baseline edits (C13-C16).**

### Class E — rc-era leftovers (pure deletions possible now)

| rank | id | what | files + lines | del | note |
|---|---|---|---|---|---|
| 52 | E01 | `destroyZeroRefNow` → inline into `destroyZeroRef` | gc.zig 6285-6289 (caller 6280) | 5 | pure |
| — | E02 | already counted: `retainObject` (A08), Realm-teardown probe (A10), gc_slot `dup/free`/`retain` (A02), `refcount_removed_headers` (C16), `Report.conservative` sink (A05), `Tracer.cycle_visited` check (B03) | — | 0 | — |
| — | E-note | **scheduled S1-S4, do not touch now**: `Phase.decref` + `beginDecrefPhase/endDecrefPhase` + `ZeroRefScratch` chain (gc.zig 846, 1732-1766, 5364-5495; object.zig 3003, stw 2297-2299; ~170, S1 step 6); `headerRefCount` family 1357-1481 (S1); `RefCountHeader`/`string_rc_prefix_size` 1233-1268 (S2); `storedListPrevious` + `listAddTail`/`listDelAfter` TraversalOwned twins 1489-1611 (~45, S1); `deinit` five-phase holding stacks 2505-2693 (189, after S1/S3); `unlinkObjectWithBytes` `cycle_visited` short-circuit 3383-3397 (S1); `BlockFlags.mark/is_pinned/cycle_visited` + `TraceHeader.pinned/setPinned` (S4 — `is_pinned` has 11 reads in gc_trace_stw and is the tracer's authority today); deferred-link helpers 1308-1332 + `DeferredFreeStack` + `cycle_deferred_frees` + `verifyDeferredFreeRunTopology` 2469-2503 (S4 step 8); object_gc.zig `trySettleTracerBlockCorpse` 51-112, `drainCycleDeferredFreesBudgeted` 114-186, weak-husk arm 33-49 (S3); BH-19 Pass-A settlement 1051-1107 (S4); BH-18 hot-reuse (~290, S4 owner); `NegativeRefCount` check 5646-5647 (S1); memory.zig:1471 `initial_lifetime_word` (S1 step 4); gc_representation_constants `metadata_rc_offset`/`free_cell_poison` cycle-visited check 49-51 (S4) | — | 0 | ≈1,500 more lines fall in S1-S4 per docs/tracing-gc-completion-plan.md |

## 2. Totals (non-test lines; ±15% — surveys overlap and were merged by hand)

| class | deletable now | notes |
|---|---|---|
| A | ≈ 2,720 | zero behaviour change; ~10 tests deleted/edited |
| B | ≈ 2,390 | B01 1,300 / B02 280 / B03 620 / B04 130 / B05 60; ~480 test lines |
| D | ≈ 2,110 (+340 conditional) | D07 is the only real refactor (med); D02 timing is the owner's (S5-early) |
| C | ≈ 1,860 (≈ 730 free, ≈ 225 owner call, ≈ 905 need tool/baseline edits) | |
| E | 5 (+≈1,500 scheduled S1-S4) | |
| **total** | **≈ 9,085** (≈ 7,500 with no owner/tool decisions) | of 16.9k GC + memory.zig + 0.7k slab_locality_audit → ≈ 8.5k remaining before S1-S4 |

Test-line removal ≈ 1,100 (mostly sticky 220, parallel 108, carrier 150, slot 115, memory counters 70).

## 3. Do not touch (looks dead, is load-bearing)

| item | evidence |
|---|---|
| `stress_collect`/`stress_cadence` (`ZJS_GC_STRESS`) gc.zig 119-125 | build/tests.zig:81 `test-gc-stress` → checkpoint-gate (gates.zig:122); mise.toml:78 test262-stress; context.zig:638, gc.zig:4703 |
| `minor_audit`/`minor_audit_fatal` (`ZJS_MINOR_AUDIT`) + **`auditUnbarrieredStore`/`UnbarrieredStoreSite`/`unbarriered_store_hits`** gc.zig 202-207, 4963-5005; sites object.zig:10154, array_ops.zig:6119, call_runtime.zig:3369 | gate `comptime !(runtime_safety or roots_diag_enabled)` → compiled into every Debug/ReleaseSafe unit-test binary and **armed by build/tests.zig:83 `ZJS_MINOR_AUDIT=fatal`**; panics at gc.zig:4999. Looks like a finished S0 L3 probe; it is a live L2 net |
| MINOR-AUDIT-WHERE block gc_trace_stw.zig 2664-2732 + object.zig `ordinaryPayloadForAudit` 6630, `cachedIteratorNextSlotForCycleGc` 2163 + `minor_audit_fatal` panic 2738 | uncommitted S0 safety-net work (`git diff HEAD`) |
| `verify_minor`/`verify_minor_fatal` (`ZJS_GC_VERIFY_MINOR`): `computeFullReachable` 386-466, VERIFY-MINOR block 2475-2500, GN-4 `conservative_only_young` | build/tests.zig:82; row `gc: conservative-only young …` parsed at gc_stats_snapshot.py:361-364; completion-plan 裁决门 trio |
| `arena_audit`/`invariantChecksEnabled` (`ZJS_GC_ARENA_AUDIT`) and everything under it: `verifyCollectorInvariants` 601-687, `verifyIntrusiveList`/`verifyCircularHeaderList` 5607-5774/1636-1680, `verifyRepresentationInvariants`+`verifyMetadataSemantics` 5786-5892/2024-2105, `verifyConstructionRoots`/`verifyGenerationInvariants`/`verifyMajorRetirementCommit` 5776-5932, `verifyHeapAccounting` core walk 5951-5977/6000-6027, `verifyDeferredFreeRunTopology` 2469-2503 + `deferred_run_topology_verified`, `verifyObjectPropertyStorageLayouts` 3907-3986, block heap `verify` 1558-1883 + `verifyBlockAllocCount` (stw 1611-1619, 2897-2900), registry `auditArenas`/`verifyIndex`/`forEachArenaBlock`, `remembered_skip_audit` (GN-5) | `if (comptime runtime_safety) return true; return arena_audit;` (gc.zig 222-225) → runs after every collection in all unit tests; tools/perf/gate_smoke.sh:110 and build/gates.zig:48 set `ZJS_GC_ARENA_AUDIT=1` in ReleaseFast |
| `verify_major_all` (`ZJS_GC_VERIFY_MAJOR_ALL`) + `IncrementalMajorScope`/`incrementalMajorScope()`/`verifyStickyCondemnation` 482-516/`noteStickyMajorOracle`/oracle block 1230-1248/marking suppression 391-397 | `if (comptime gc.sticky_major_enabled or gc.roots_diag_enabled)` — shared with R3; deleting them with B02 breaks the `-Dzjs_gc_roots_diag=true` build |
| `-Dzjs_gc_roots_diag` / `roots_diag_enabled`: gc_conservative.zig 389-617 census, gc_trace_stw.zig 423-461, 2207-2223 `recordConservativeCandidate`, cli 436-438, `Registry.roots_diag` (void by default) | in-flight owner-approved R3 (docs/tracing-gc-s0-spec.md §L4, completion-plan:5,145); rows parsed by untracked `tools/perf/r3_summarize.py:17,32,36` |
| Every `--gc-stats` row in gc_stats_snapshot.py:237-437 (`one_match` → SnapshotError): collections/freed/failed, collector outcome, heap bytes + `account peak`, major pause (`retained N of M`), allocation histogram, block heap committed/…/`large maps`, deferred block runs/hot reuse/reopened/bitmap reclaimed cells, major threshold resets, page returns, decommit checks, process heap trim, incremental subphase totals + work totals, generation current young, minor collections, retirement commits/abandons/state, generational barrier, marking barrier, incremental doomed, minor STW total, minor pause (2 variants), minor phase totals, minor young-at-start, conservative-only young (2 variants), incremental major cycles, cycle envelope, STW phase-segment max, STW phase totals, marked-set census family (15 rows: census/kinds/trace classes, 6 `mark storage`, 5 `mark trace class storage`, `inline property upper slots` + `inline ordinary property upper slots`) | `mise run stage0` (mise.toml:41-47 → stage0_screen.py:202-222). gate_smoke_check.py:37-53 additionally requires retirement, `endpoint`/`settled doomed_pending`, block heap committed (`mise run gate`) |
| `detailed_reports` switch (stw 518-542), `doomedStateSnapshot` 1468-1512, incremental subphase timers (CC-5), `SliceKind`/`recordMajorSlicePause` (C3), barrier counters `generationalBarrierDetailed`/`shadeForConcurrentMark` report (C11/C12), `threshold_growth_hits/floor_hits`, `liveCount`/`liveCountKind` (57/14 test oracles; `memoryUsage`), generation retirement counters, `PauseDistribution`/`GCPauseDistribution`/`JSRuntime.gcPauseDistribution()` (public API src/root.zig:11) | all feed parsed rows or public API |
| `peak_allocated_bytes` + `samplePeakAtCollection` (memory.zig 1983-1985; runtime.zig 3019/3121/3301); `allocation_count`/`hasOutstandingAllocations` (runtime.zig 1749-1762 leak net); `limit_gc_fn` collect-and-retry 1946-1958; `trigger_gc_fn` hook + `-Dzjs_force_gc` tier | `account peak` row :247; leak net; 27 test refs |
| `block_header_bytes` memory.zig 263-264 | prod use at memory.zig:756 (`accountedMallocSize`) |
| `hot_reuse_k64` gc_block_heap.zig:74; `Superblock.page_bits` 204-212; `Block.sweep_state` field is production storage (D01 must relocate `SweepState`) | 76 → openBlock 2251; classed nonempty index used by `noteNonemptyBlock`/`publishCompletedHotBlocks`/verify |
| `MarkStack.pop` (non-prefetch) gc_mark_queue.zig 246-254 | prod caller `popSegmentedFrontier(…, false)` stw:2243 |
| `gc.StringHeader`/`RefCountHeader`/`string_rc_prefix_size` gc.zig 1233-1268 | live string rc prefix (string.zig ×10, value.zig ×5, gc.zig:6254) — S2 |
| `is_pinned` header bit | 11 reads in gc_trace_stw are the tracer's pin authority today; `pin_entries` only consulted by verify — S4 must reroute first |
| `src/gc_representation.zig` (173) + `src/gc-representation-trace-snapshot.txt` + build step `gc-representation-snapshot` | **gc-core-1 L3 "A" is refuted**: core.zig:10-20 compares `snapshot_text` against the embedded baseline (verified now) — it is the layout-drift oracle; D04 must rewrite its catalog reads and regenerate the txt |
| gc_conservative.zig platform arms (x86_64 SysV 126-136/279-315/424-429, Darwin 168-180, Windows 182-198) | CI: linux-x86_64 checkpoint-gate unit-tests the x86_64 arm; macos-14/windows-2025 build+smoke; nightly ships both artifacts |
| `Block.forgetDoomedCell` 520-526 + call 2209; weak-husk guards stw 1577-1584/2886-2893 | uncommitted production bug fix (WeakRef husk freed mid-drain under ZJS_GC_STRESS=1) |
| `externalMemoryRequestReason` `.allocation_debt` branch (gc.zig:2784); Policy `large_object_threshold`/`external_weight`/`major_debt_threshold`/`native_cleanup_slice_jobs`; `PinEntry`/`construction_pin_count`/`pinHeader`; `NonBlockObjectAuthority` itself; `pinEntryIsConstructionRoot`; weak-husk helpers 1421-1447; `containsHeader` (plugin `ownsObject`); `remap` (object.zig:4934); decommit/trim BH-20; hot-reuse BH-18 | production paths |
| `-Dzjs_ownership_audit` build option | atom quarantine (atom.zig 18/21/992, class.zig 418) + nightly.yml:40 — only its carrier half (B01) goes |

## 4. Refuted / reclassified during verification (do not re-propose as listed)

- gc-core-1 L3 `gc_representation.zig` A → **keep** (tested baseline).
- trace-stw C3 mark-footprint "one-off" → **load-bearing Stage-0 tool** (C16, owner call); C2 census timing conditional on it.
- trace-stw B2 roots_diag → **in-flight R3**, not a candidate; C7 WHERE-block → in-flight S0.
- trace-stw A8 / gc-core-1 V13 injection harness A → **C** (documented manual mutation procedure); only mutation-2 arm (gc.zig 3113-3117) is unreachable everywhere.
- gc-core-2 A12 "duplicate doc" → **MOVE** (only written rationale for barrier direction).
- gc-core-1 E9 `last_failure`/`last_collection_time_ns` keep → **dead** (D05); V10/C-5 unbarriered-store probe "job done" → **safety net**.
- cross-cutting D-2 sweep model "pure file deletion" → **collapse** (Block.sweep_state); B-1 sticky doc list → wrong files (real ones: every2-arm, splay-account); M6 "no tools" → two non-gate perf tools; SP-05 `metadata_prefix_size` → folds into D08; `block_header_bytes` → live; BH-15 "identical bodies" → wrapper (same action); R5 test ref → 0; `headerMarked` shape.zig callers → stale (none).
- Line drift to apply: gc_trace_stw.zig +8 after ~1577 / +16 after ~2886 vs the surveys; core.zig −2 for refs ≥16000; gc.zig zero drift.

## 5. Suggested batch order (each batch ends with the §0.3 gate)

1. §0.1 regex fix → **batch 1**: A01, A02(+C19 decision), A16, A15, A14, A08, A13, A12, C05, C06, C10 (pins), C18 (comments).
2. **batch 2**: B01 carrier → B02 sticky (oracle slot rename; keep roots_diag carve-outs) → C09, D08.
3. **batch 3**: B03 parallel (relocate `footprint`) → D03 mark-queue → D09 (FrontierSafeHeader with it).
4. **batch 4**: A04, A05, D01 sweep model, D05 stats, D06, C01, C04, C07, C08 → then D07 majors-unify as its own batch with test262-stress.
5. **batch 5** (owner decisions): D02 gate collapse timing (S5-early), A11+B04 Policy API, B05, C02, C03, C11, C12, D10-medium, D12, D13.
6. **batch 6** (tool-contract changes, one coordinated PR + baseline re-freeze): C13, C14, C15, C16.