[
  {
    "source": "src/core/string.zig",
    "pattern": "catch-unreachable-alloc",
    "contains": "return self.flatten() catch @panic(\"zjs: out of memory while flattening string rope\");",
    "reason": "rope flatten last resort: borrowed-slice readers (resolveData) cannot propagate errors; flattenInfallible retries after one object-cycle collection and only a second allocation failure aborts (eecf6c8)",
    "exit_milestone": "make the remaining borrowed-slice readers rope-aware or fallible (String.ensureFlat first) so the infallible flatten backstop can be deleted"
  }
]
