[
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-return",
    "contains": "const atom_id = identifierLikeAtom(s);",
    "reason": "same shape as audit B-1: labelStartAtom forwards identifierLikeAtom's borrowed id to its caller; the break/continue label path (8c8787cd) dups before advance, so the owner lives at the call site and not in this helper",
    "exit_milestone": "fold into audit §6.3 with identifierLikeAtom: return an owned atom (labelStartAtomOwned) or state the borrow with a // borrowed-atom: contract on the return, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-return",
    "contains": "const atom_id = s.token.payload.ident.atom;",
    "reason": "audit B-5 root (§3.2): classNameAtom returns the class-name id borrowed from s.token; its two callers dup into class_name before advance, so parseClass supplies the owner this helper never takes",
    "exit_milestone": "audit §6.1 - have parseClass receive an owned class name (classNameAtomOwned) so the helper stops publishing a borrow, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-return",
    "contains": "return if (s.peekKind() == tok.TOK_IDENT) s.token.payload.ident.atom",
    "reason": "audit B-1 (docs/borrowed_atom_audit.md §3.2): identifierLikeAtom hands back the current token's atom with no contract in its name or signature; all 16 call expressions happen to establish an owner before advance, so it is correct today by 16 independent conventions rather than by construction",
    "exit_milestone": "audit §6.3 - add an identifierLikeAtomOwned companion and migrate the call sites that keep the name past advance (a bare rename to ...Borrowed does not retire this entry: the checker only exempts ...Owned or an explicit // borrowed-atom: contract on the return), then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-state-store",
    "contains": "const atom_id = identifierLikeAtom(s);",
    "reason": "same shape as audit B-2: the `using` binding name is borrowed, survives advance and is published through s.pending_function_name; the retain comes from the var row defineVar established just before advance (FunctionDef.appendVar, bytecode.zig:3300)",
    "exit_milestone": "audit §6.4 - dup + defer free before defineVar so the using declarator owns its name, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-state-store",
    "contains": "const enum_atom = s.token.payload.ident.atom;",
    "reason": "audit B-3 (§3.2): the TS enum name is borrowed from s.token, used after advance and parked in s.last_declared_atom; the only retain is the one addScopeVar leaves behind, so correctness depends on statement order plus a downstream sink's dup",
    "exit_milestone": "audit §6.4 - dup + defer free locally before addScopeVar AND make last_declared_atom an owned field (a local dup alone converts this finding into the rule-D shape), then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-state-store",
    "contains": "const name_atom = classNameAtom(s) orelse return Error.UnexpectedToken;",
    "reason": "audit B-5 (§3.2): s.last_class_decl_atom keeps classNameAtom's borrowed id; liveness is spliced from two unrelated owners (parseClass's local class_name dup, then the class binding's var row) and the field itself never retains",
    "exit_milestone": "audit §6.1 - make last_class_decl_atom an owned field (free on overwrite and in State.deinit) or return the class name owner from parseClass, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-state-store",
    "contains": "const ns_atom = s.token.payload.ident.atom;",
    "reason": "audit B-4 (§3.2): the TS namespace name is borrowed from s.token and stored into both s.current_namespace_atom and s.last_declared_atom after advance; the scope var row created by addScopeVar is the only owner",
    "exit_milestone": "audit §6.4 - dup + defer free locally, and make current_namespace_atom / last_declared_atom owned fields, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-use-after-release",
    "contains": "const atom_id = identifierLikeAtom(s);",
    "reason": "same shape as audit B-2: the for-in/of binding name is borrowed and emitted after advance; the retain is the one defineVar / declareForInOfVarBinding leaves in the var row (bytecode.zig:3300)",
    "exit_milestone": "audit §6.4 - dup + defer free before the defineVar call in parseForInOf, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-use-after-release",
    "contains": "const catch_atom = if (s.peekKind() == tok.TOK_IDENT)",
    "reason": "audit B-2 (§3.2): the catch binding is borrowed from s.token and emitted after advance; it stays alive only because defineVar created or reused a var row one statement earlier and that row holds the retain (FunctionDef.appendVar, bytecode.zig:3300)",
    "exit_milestone": "audit §6.4 - dup + defer free before defineVar so the catch clause owns its binding name, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-use-after-release",
    "contains": "const direct_lhs_atom: ?Atom = if (isIdentifierLikeToken(s)) identifierLikeAtom(s) else null;",
    "reason": "same shape as audit B-1/B-2: the assignment LHS name is borrowed before parseCondExpr, which consumes the token, and is read afterwards for anonymous-function naming; the retain is held elsewhere - emitScopeGetVar's atom operand, and for a direct lvalue the transfer into LValue.name with owns_name=true - never by this local",
    "exit_milestone": "dup direct_lhs_atom + defer free at the point of capture (qjs js_parse_assign_expr2 keeps its own reference) so the naming path does not depend on the lvalue's retain, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-use-after-release",
    "fn": "parseArrowFunction",
    "contains": "const param_atom = identifierLikeAtom(s);",
    "reason": "same shape as audit B-2: the arrow parameter name is borrowed and reused after advance for the default initializer; the owner is the dup appendArrowParamBindingName -> appendOwnedParserAtom put into param_names (freed by deinitOwnedParserAtoms), not the local",
    "exit_milestone": "read the owned copy (param_names last item) - or hold a local dup + defer free - for every use after advance, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "borrowed-use-after-release",
    "fn": "parseFunctionParameters",
    "contains": "const param_atom = identifierLikeAtom(s);",
    "reason": "same shape as audit B-2: the simple parameter name is borrowed and reused after advance for the default initializer; the owner is the dup appendOwnedParserAtom already put into parameters.simple_names (freed by FunctionParameters.deinit), not the local",
    "exit_milestone": "read the owned copy (parameters.simple_names last item) - or hold a local dup + defer free - for every use after advance, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "owned-escape-state-store",
    "contains": "s.last_declared_atom = name_atom;",
    "reason": "same shape as audit B-6: parseFunctionDecl parks its defer-freed name dup in s.last_declared_atom, which is read later by the TS namespace paths (§3.2 B-4); after the defer runs the id is owned by unrelated third parties - the declaration's var row and FunctionDef.init's func_name dup (bytecode.zig:3045)",
    "exit_milestone": "audit §6.4 - make last_declared_atom an owned field (free on overwrite and in State.deinit) so every writer transfers ownership, then drop this entry"
  },
  {
    "source": "src/parser.zig",
    "pattern": "owned-escape-state-store",
    "contains": "s.last_var_decl_atom = atom_id;",
    "reason": "audit B-6 (§3.2): the field keeps parseVar's dup id after that dup is freed at the end of the declarator; harmless today only because the field is write-only (no reader in the tree), which makes it a trap that a future reader turns into a class-C use-after-free",
    "exit_milestone": "audit §6.2 - delete the write-only last_var_decl_atom field, then drop this entry"
  }
]
