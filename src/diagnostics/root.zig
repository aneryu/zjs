//! Structured diagnostics (SourceText, Span, Label, ErrorReport, StackTrace,
//! SourceMap). Currently minimal scaffold.
//!
//! See docs/fun_zjs_subtree_architecture.md §3 and runtime-mvp.md.

pub const SourceText = struct {};
pub const Span = struct {};
pub const Label = struct {};
pub const ErrorReport = struct {};
pub const StackTrace = struct {};
pub const SourceMap = struct {};
