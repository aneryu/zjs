diff --git a/src/core/profile.zig b/src/core/profile.zig
index 81ae3633..28a931cb 100644
--- a/src/core/profile.zig
+++ b/src/core/profile.zig
@@ -22,6 +22,19 @@ pub const OpcodeProfile = struct {
     global_lookup_count: u64 = 0,
     alloc_count: u64 = 0,
     call_frame_count: u64 = 0,
+    call_return_count: u64 = 0,
+    l0_entry_count: u64 = 0,
+    fallback_native_reentry_count: u64 = 0,
+    c_function_call_count: u64 = 0,
+    native_call_count: u64 = 0,
+    backtrace_publish_count: u64 = 0,
+    backtrace_restore_count: u64 = 0,
+    native_fence_count: u64 = 0,
+    native_fence_restore_count: u64 = 0,
+    entry_republication_count: u64 = 0,
+    reload_top_count: u64 = 0,
+    reload_after_pop_count: u64 = 0,
+    interrupt_poll_count: u64 = 0,
     /// Delta-attribution state for the tail-call threaded dispatcher (see
     /// exec/vm_profile.zig): the currently-open opcode interval, closed by
     /// the next table dispatch or by `flushPendingDispatch`.
diff --git a/src/cli/zjs.zig b/src/cli/zjs.zig
index bb96b4b2..5b149b69 100644
--- a/src/cli/zjs.zig
+++ b/src/cli/zjs.zig
@@ -616,6 +616,19 @@ fn dumpPerfJsonOpcodeProfile(output: *std.Io.Writer, profile: *const zjs.OpcodeP
     try output.print("    \"global_lookups\": {d},\n", .{profile.global_lookup_count});
     try output.print("    \"allocations\": {d},\n", .{profile.alloc_count});
     try output.print("    \"call_frames\": {d},\n", .{profile.call_frame_count});
+    try output.print("    \"call_returns\": {d},\n", .{profile.call_return_count});
+    try output.print("    \"l0_entries\": {d},\n", .{profile.l0_entry_count});
+    try output.print("    \"fallback_native_reentries\": {d},\n", .{profile.fallback_native_reentry_count});
+    try output.print("    \"c_function_calls\": {d},\n", .{profile.c_function_call_count});
+    try output.print("    \"native_calls\": {d},\n", .{profile.native_call_count});
+    try output.print("    \"backtrace_publishes\": {d},\n", .{profile.backtrace_publish_count});
+    try output.print("    \"backtrace_restores\": {d},\n", .{profile.backtrace_restore_count});
+    try output.print("    \"native_fences\": {d},\n", .{profile.native_fence_count});
+    try output.print("    \"native_fence_restores\": {d},\n", .{profile.native_fence_restore_count});
+    try output.print("    \"entry_republications\": {d},\n", .{profile.entry_republication_count});
+    try output.print("    \"reload_top\": {d},\n", .{profile.reload_top_count});
+    try output.print("    \"reload_after_pop\": {d},\n", .{profile.reload_after_pop_count});
+    try output.print("    \"interrupt_polls\": {d},\n", .{profile.interrupt_poll_count});
     try output.writeAll("    \"opcodes\": [");
     for (rows[0..row_count], 0..) |row, index| {
         if (index != 0) try output.writeByte(',');
diff --git a/src/core/context.zig b/src/core/context.zig
index baf05e8b..d39147a1 100644
--- a/src/core/context.zig
+++ b/src/core/context.zig
@@ -642,6 +642,7 @@ pub const JSContext = struct {
     /// resets the counter and runs the handler (a hit leaves the counter ≤0,
     /// so the cold re-poll still triggers the slow leg).
     pub inline fn pollInterruptTick(self: *JSContext) bool {
+        if (self.runtime.opcode_profile) |profile| profile.interrupt_poll_count +|= 1;
         self.interrupt_counter -= 1;
         return self.interrupt_counter <= 0;
     }
diff --git a/src/exec/vm_call.zig b/src/exec/vm_call.zig
index cc0bc334..8d8eb26d 100644
--- a/src/exec/vm_call.zig
+++ b/src/exec/vm_call.zig
@@ -71,6 +71,7 @@ pub const CallProfileGuard = if (build_options.zjs_enable_opcode_profile) struct
 
     pub fn deinit(self: @This()) void {
         if (self.rt.opcode_profile != null) {
+            if (self.rt.opcode_profile) |profile| profile.call_return_count +|= 1;
             _ = core.profile.activate(self.previous);
         }
     }
diff --git a/src/exec/builtin_dispatch.zig b/src/exec/builtin_dispatch.zig
index c4f0b13c..beb8ba8c 100644
--- a/src/exec/builtin_dispatch.zig
+++ b/src/exec/builtin_dispatch.zig
@@ -238,6 +238,10 @@ pub const NativeBacktraceScope = struct {
 
     pub fn push(self: *NativeBacktraceScope) void {
         std.debug.assert(!self.active);
+        if (self.ctx.runtime.opcode_profile) |profile| {
+            profile.native_call_count +|= 1;
+            profile.backtrace_publish_count +|= 1;
+        }
         self.frame = .{
             .data = &self.data,
             .resolver = resolveNativeBacktrace,
@@ -248,6 +252,7 @@ pub const NativeBacktraceScope = struct {
 
     pub fn deinit(self: *NativeBacktraceScope) void {
         if (!self.active) return;
+        if (self.ctx.runtime.opcode_profile) |profile| profile.backtrace_restore_count +|= 1;
         self.ctx.popActiveBacktraceFrame(&self.frame);
         self.active = false;
     }
@@ -265,6 +270,7 @@ pub inline fn preflightCFunctionCall(
 ) HostError!void {
     const object = func_obj orelse return;
     if (object.class_id != core.class.ids.c_function) return;
+    if (caller_ctx.runtime.opcode_profile) |profile| profile.c_function_call_count +|= 1;
     const planned_stack_bytes = std.math.mul(
         usize,
         formal_length,
diff --git a/src/exec/inline_calls.zig b/src/exec/inline_calls.zig
index f454ad65..52b8035d 100644
--- a/src/exec/inline_calls.zig
+++ b/src/exec/inline_calls.zig
@@ -1088,6 +1088,7 @@ pub const NativeBoundaryScope = struct {
     }
 
     pub fn push(self: *NativeBoundaryScope) void {
+        if (self.rt.opcode_profile) |profile| profile.native_fence_count +|= 1;
         self.outer_view.freeze(self.view.bottom_exclusive);
         self.frame = .{
             .data = &self.view,
@@ -1142,6 +1143,7 @@ pub const NativeBoundaryScope = struct {
     }
 
     inline fn popBacktrace(self: *NativeBoundaryScope) void {
+        if (self.rt.opcode_profile) |profile| profile.native_fence_restore_count +|= 1;
         if (comptime builtin.mode == .ReleaseFast) {
             // Successful and error exits both unlink this stack-local node
             // permanently. Avoid clearing dead fields on the ReleaseFast hot
diff --git a/src/exec/zjs_vm.zig b/src/exec/zjs_vm.zig
index 03628410..388dd959 100644
--- a/src/exec/zjs_vm.zig
+++ b/src/exec/zjs_vm.zig
@@ -520,6 +520,11 @@ fn runWithArgsState(
     entry_initial_pc: usize,
     entry_prepared_frame: ?*const PreparedEntryFrame,
 ) HostError!core.JSValue {
+    if (ctx.runtime.opcode_profile) |profile| {
+        profile.l0_entry_count +|= 1;
+        if (ctx.runtime.active_invocation != null and ctx.runtime.active_native_call != null)
+            profile.fallback_native_reentry_count +|= 1;
+    }
     const call_profile_guard = call_vm.enterCallProfile(ctx.runtime);
     defer call_profile_guard.deinit();
 
diff --git a/src/exec/tailcall_dispatch.zig b/src/exec/tailcall_dispatch.zig
index 1bae0317..d82ae6b5 100644
--- a/src/exec/tailcall_dispatch.zig
+++ b/src/exec/tailcall_dispatch.zig
@@ -463,6 +463,7 @@ noinline fn iteratorNextCallSetupRecover(vm: *Vm, depth: u8, err: HostError) boo
 /// no tail_request staging, no driver-side spill/reload detour. Expanded
 /// inline into the (Handler-signature) caller so the tail calls are legal.
 inline fn enterEntry(vm: *Vm, entry: *inline_calls.Entry, code_ptr: [*]const u8) Outcome {
+    if (vm.rt.opcode_profile) |profile| profile.entry_republication_count +|= 1;
     // Enter the entry pushCall handed back instead of reloading
     // `machine.top` — qjs enters the callee via the alloca result pointer
     // already in a register (quickjs.c:17846); this is the equivalent
@@ -5238,6 +5239,7 @@ const resident_tail_table = [3]Handler{
 // ===========================================================================
 
 fn reloadTop(vm: *Vm, pc: *[*]const u8, sp: *[*]JSValue, var_buf: *[*]JSValue) void {
+    if (vm.rt.opcode_profile) |profile| profile.reload_top_count +|= 1;
     vm.machine.loadCurrentLevel(&vm.frame, &vm.stack, &vm.catch_target);
     vm.var_refs_base = vm.frame.var_refs.ptr;
     vm.function = vm.frame.function;
@@ -5261,6 +5263,7 @@ inline fn reloadAfterPop(
     sp: *[*]JSValue,
     var_buf: *[*]JSValue,
 ) void {
+    if (vm.rt.opcode_profile) |profile| profile.reload_after_pop_count +|= 1;
     if (caller_entry) |entry| {
         vm.frame = &entry.frame;
         vm.var_refs_base = entry.frame.var_refs.ptr;
diff --git a/src/exec/vm_profile.zig b/src/exec/vm_profile.zig
index 96ec74be..b97c2f1f 100644
--- a/src/exec/vm_profile.zig
+++ b/src/exec/vm_profile.zig
@@ -16,7 +16,9 @@ const core = @import("../core/root.zig");
 pub const enabled = build_options.zjs_enable_opcode_profile;
 
 pub inline fn noteDispatch(rt: *core.JSRuntime, opcode: u8) void {
-    if (comptime !enabled) return;
-    const profile = rt.opcode_profile orelse return;
-    profile.noteDispatch(opcode);
+    // CALL-BOUNDARY-DIAGNOSIS counter-only build: retain the profiling
+    // attachment used by boundary counters, but remove per-opcode clocks.
+    // This binary is frequency-only and forbidden for all timing use.
+    _ = rt;
+    _ = opcode;
 }
