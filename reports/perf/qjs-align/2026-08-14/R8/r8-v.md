# R8-V PRICES — L0–L3 值/分派地板

lane: R8-V / CPU 5 / 诊断批，src 只读  
zjs pad0 `12bc8b8a…` / pad3 `542965de…` / pad7 `e9fe8f66…`  
qjs `b76d1542…`  
8-sample ABBA。路径核证：zjs-profile `--perf-json`。

## 结论先行

**06-26 的 L1–L3 ~2× 地板已经不在。** 空循环 0.779，int add 0.798，or/sar 0.865。  
TS / richards「删到空仍保持比」**不是**这些原语的单价——单价预测它们 zjs 该赢。欠账是涌现（见 RECONCILE）。

## 价目（ctrl 差分，cyc/次额外工作）

| id | 形状 | N | cyc z/q | z/次 | q/次 | Δ (z−q) | 路径核证 |
|---|---|---:|---:|---:|---:|---:|---|
| V1 | 空循环 | 2e8 | **0.7788** | 14.37 | 18.46 | −4.08/iter | 6 op：`inc_loc if_false8 get_arg0 get_loc0 lt goto8` ×N |
| V2 | `s = i` | 1.5e8 | 0.7728 | +3.36 | +4.49 | **−1.13** | 多 `get_loc0`/`put` |
| V3 | `s = s + 1` | 1.5e8 | 0.7980 | +3.89 | +4.44 | **−0.54** | 热 op 是 **`add_loc`**（融合，不是 generic `add`） |
| V3b | `s = s + a` | 1.5e8 | 0.7824 | +3.35 | +4.20 | −0.85 | `add_loc` |
| V4 | `s = s + 0.5` | 8e7 | 0.8505 | +10.39 | +10.66 | −0.28 | `add` + `push_const8`（float） |
| V5 | `s = s \| 1` | 1.5e8 | 0.8644 | +8.85 | +8.41 | **+0.44** | `or`+`push_1`+get/put |
| V6 | `s = s >> 1` | 1.5e8 | 0.8646 | +8.85 | +8.41 | **+0.44** | `sar` |
| V7 | `s = s * 3` | 8e7 | 0.8796 | +9.38 | +8.55 | +0.83 | `mul` |

分派地板单价（V1 / 6 op）：**z 2.40 / q 3.08 / Δ −0.68 cyc/cheap-op**。

## 三 pad（结论条）

| | pad0 n=8 | pad3 n=4 | pad7 n=4 |
|---|---:|---:|---:|
| V1_ctrl | 0.7788 | 0.7720 | 0.7740 |
| V5_int_or | 0.8644 | 0.8586 | 0.8560 |

零翻转。or/sar 是本层唯一稳定的微弱 zjs 税（+0.44），幅度远不够撑 richards 1.11。

## 对账含义

richards 5.0e8 opcode × −0.68 ≈ **−340M** 预测（zjs 该快）。实测超出 **+448M**。  
本层闭合为负。修复方向不在 loc/int handler 体（R5 已证 ZJS-ADVANTAGE）。

## 不要做

- 不要按「对齐更胖的 qjs CASE」去改 `get_loc0`/`add_loc`。
- 不要把 V5 +0.44 立成 ≥1.0 项。
