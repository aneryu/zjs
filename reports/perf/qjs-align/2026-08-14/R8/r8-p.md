# R8-P PRICES — L6 属性 / 形状

lane: R8-P / CPU 7 / 诊断批，src 只读  
锚点：crypto · EB · TS。8-sample。三 pad 给结论条。

## 结论先行

1. **own 数据读是 ZJS-ADVANTAGE（0.777，Δ −4.1 cyc）**，三 pad 零翻转。禁止「对齐」qjs。
2. own 写 0.851、原型读 0.809、miss 0.882、链读 0.876：全部 zjs 不亏或小胜。
3. **形状转换 / add-tail 后写是本层唯一稳定正税**：`{x:1}; o.y=2` **1.114，Δ +30 cyc**（三 pad 1.114/1.110/1.117）。
4. 3 字段字面量 0.897（Δ −28）——定义字段 zjs 更快。

X-10 之后 miss 不再是 +180M 那种账。P5 miss 0.882。

## 价目

| id | 形状 | N | cyc z/q | z/次 | q/次 | Δ | 路径 |
|---|---|---:|---:|---:|---:|---:|---|
| P1 | `o.x` own | 4e7 | **0.7770** | 15.8 | 20.5 | **−4.64** | `get_field`=N |
| P2 | `o.x = i` own | 6e7 | 0.8506 | 14.2 | 15.2 | −0.99 | `put_field`=N |
| P3 | 原型数据读 | 4e7 | 0.8087 | 20.5 | 24.7 | −4.22 | `get_field`=N |
| P4 | 4 深链 | 1e7 | 0.8764 | 30.9 | 33.3 | −2.35 | `get_field`=N |
| P5 | miss `o.z` | 2e7 | 0.8824 | 17.3 | 17.5 | −0.19 | `get_field`=N，无 throw |
| P6 | `{}`+3 新属性 | 5e6 | 1.0114 | 315 | 308 | +7.8 | `object`+3×`put_field` |
| P7 | `{}` | 5e6 | 1.0580 | 78 | 69 | +9.1 | 同 M3 |
| P8 | `{x,y,z}` | 5e6 | 0.8968 | 266 | 294 | −28 | `define_field`=3N |
| P9 | `{x}; o.y=2` | 5e6 | **1.1142** | 240 | 210 | **+30.2** | `define_field`+`put_field` |

(P6−P7)/3 ≈ 新属性写：(315−78)/3 = 79 z vs (308−69)/3 = 80 q ≈ 平。  
P9 比「纯 add-tail」贵，因为字面量形状后再转。

## 三 pad

| | pad0 | pad3 | pad7 |
|---|---:|---:|---:|
| P1 own read | 0.7770 | 0.7812 | 0.7816 |
| P9 shape trans | 1.1142 | 1.1097 | 1.1173 |

## 对账含义

get_field 负单价 × TS 17% 份额 → 预测 TS property **帮 zjs**。R4-C property 两侧 ~37% 闭合，与「不要修 TS get_field」一致。  
P9 频次在 zoo 里远低于 own 读，不够单独立项。

## 不要做

- 不要动 `get_field` 命中臂。
- 不要重开 X-10 / slow-property。
